# G16 Synthetic Demo Journey Hub

## Current status

This directory is the documentation-only shell for package version `1.0` of one isolated, manual, development-only responsive all-at-once synthetic demo journey hub.

- Authorization date: `2026-09-24`
- Baseline branch: `development/sprint-7-matching-g16-synthetic-demo-journey-hub`
- Baseline commit: `9408fbda474ecdb61936956b1f01f9c0d1fa0ec3`
- Authorization status: `DOCUMENTATION_ONLY_AUTHORIZED`
- Code phase status: `BLOCKED_PENDING_INDEPENDENT_AUDIT_AND_HUMAN_CONFIRMATION`
- Frozen allowlist (LF-only line endings, UTF-8 no BOM): `G16_FILE_ALLOWLIST_v1.0.json`
- Frozen allowlist SHA-256 (exact bytes of the LF-only file): `f7f552ce0487e9a961ee52b83f2ef8c78f4bcc3067add8a239f80fab6ec82a74`

Only this README, the frozen allowlist and the authorization record are present in the documentation phase. Every path marked `planned` must remain absent until these three artifacts pass an independent audit and a subsequent explicit human confirmation authorizes the code phase.

This package is non-canonical, creates no `XFR` ID and requires no Inventory change. G7, G8, G9, G10, G11, G12, G13, G14 and G15 remain byte-for-byte and semantically unchanged.

## What "hub" means here

The future hub is a separate sibling page, manually accessible in development only at `http://127.0.0.1:5173/synthetic-demo-journey-hub.html`. Its HTML title and always-visible page title are exactly `SYNTHETIC DEMO JOURNEY HUB — NOT PRODUCTION APPROVED`. The other always-visible line is exactly `MANUAL DEV-ONLY HUB — NOT AN APPLICATION MENU`.

`hub` denotes an accessible, responsive, all-at-once page layout only. It is a manually opened development-only navigation aid that shows every region of one synthetic demo journey at the same time on one page. It is never a stepper, never a wizard and never a progression: it has no `NEXT`, no `BACK`, no current-region hiding, no reveal-on-advance and no automatic transition. The always-visible chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION` states this explicitly. The layout denotes no geography, tenancy, deployment, segmentation or routing, and no such meaning may be inferred from it. The hub is never an application menu and never a production entry point.

## All-at-once regions

The hub shows all of its regions at the same time. It links to the eight existing G8, G9, G10, G11, G12, G13, G14 and G15 manual dev-only pages and reuses the unchanged G7 Safe Presentation rendering of the unchanged G8 synthetic input. It embeds no G9, G10, G11, G12, G13, G14 or G15 page component. The regions are:

1. `SYNTHETIC DEMO JOURNEY — EIGHT MANUAL DEV-ONLY PAGES` — the journey region with exactly eight entries in fixed order, each carrying `ENTRY n OF 8`, its exact frozen existing page title, its exact visible local dev path and exactly one same-tab native anchor, with the adjacent chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION`;
2. `SAFE PRESENTATION — UNCHANGED G7/G8 REUSE` — the unchanged G7 Safe Presentation rendering of the unchanged G8 synthetic input;
3. `WHAT THIS HUB IS NOT — FIXTURE COPY` — the boundary panel with the adjacent line `FIXTURE COPY — NOT A STATUS CLAIM`;
4. `NON-OCCURRENCE RESULT` — the always-visible non-occurrence result, never a ledger and never persistent.

## The eight frozen journey entries

The eight journey entries, in fixed order, each with its exact frozen existing page title and exact local dev path, are:

1. `ENTRY 1 OF 8` — `SYNTHETIC SAFE PRESENTATION DEMO — NOT PRODUCTION APPROVED` — `/safe-presentation-synthetic-demo.html`;
2. `ENTRY 2 OF 8` — `SYNTHETIC DEAL STORY DEMO — NOT PRODUCTION APPROVED` — `/synthetic-deal-story-demo.html`;
3. `ENTRY 3 OF 8` — `SYNTHETIC DEAL STORY WALKTHROUGH — NOT PRODUCTION APPROVED` — `/synthetic-deal-story-walkthrough.html`;
4. `ENTRY 4 OF 8` — `SYNTHETIC DEAL STORY REHEARSAL — NOT PRODUCTION APPROVED` — `/synthetic-deal-story-rehearsal.html`;
5. `ENTRY 5 OF 8` — `SYNTHETIC TEST-DEAL FLOW HARNESS — NOT PRODUCTION APPROVED` — `/synthetic-test-deal-flow-harness.html`;
6. `ENTRY 6 OF 8` — `SYNTHETIC MATCHING FLOW CONSOLE — NOT PRODUCTION APPROVED` — `/synthetic-matching-flow-console.html`;
7. `ENTRY 7 OF 8` — `SYNTHETIC MATCH REVIEW WORKSPACE — NOT PRODUCTION APPROVED` — `/synthetic-match-review-workspace.html`;
8. `ENTRY 8 OF 8` — `SYNTHETIC DEAL REHEARSAL BOARD — NOT PRODUCTION APPROVED` — `/synthetic-deal-rehearsal-board.html`.

Each entry must be simultaneously visible with its `ENTRY n OF 8` label, its exact frozen existing page title, its exact visible local dev path, and exactly one same-tab native anchor whose `href` is exactly that entry's local dev path. No external URL, `target="_blank"`, `download`, query, hash, script URL, dynamic href, router, production menu or production entry point is authorized. G7 is foundation only and is not a ninth entry. The eight titles and local dev paths reuse the exact bytes frozen in the G8–G15 allowlists and page HTML and are verified by future tests.

## Pre-authored fixture copy and safe presentation reuse

Every visible evidence or reason row is exact pre-authored fixture copy that reuses only the frozen abstract tokens and carries the always-adjacent exact label `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`. Rows are fixtures only. They are not requests, records, candidates, matches, rankings or outcomes, and they never trigger matching, scoring, confidence, qualification, risk, ranking, thresholding, contact, campaign launch, deal execution or outcome recording. Computed evidence and recommendations are prohibited.

The `SAFE PRESENTATION — UNCHANGED G7/G8 REUSE` region reuses the existing G7 Safe Presentation component unchanged and passes it the existing G8 synthetic input unchanged. G7 blocked/no-echo behavior and the exact blocked output `SYNTHETIC PRESENTATION BLOCKED` remain unchanged, and the rendered lines remain exactly `SYNTHETIC REFERENCE — NOT PRODUCTION APPROVED`, `SYNTHETIC_HEADING` and `SYNTHETIC_MESSAGE`.

## Boundary panel

The boundary panel is titled exactly `WHAT THIS HUB IS NOT — FIXTURE COPY` and carries the always-adjacent line `FIXTURE COPY — NOT A STATUS CLAIM`. Its rows are exactly the six re-expressed G15 boundary rows:

- `NOT A MATCH`;
- `NOT A SCORE OR CONFIDENCE`;
- `NOT A QUALIFICATION OR RISK DECISION`;
- `NOT A RANKING OR RECOMMENDATION`;
- `NOT A CONTACT, CAMPAIGN LAUNCH, DEAL OR OUTCOME`;
- `NOT PRODUCTION APPROVED`.

The panel is fixture copy only. It is a status disclaimer and not a status claim, and it grants no business, legal, financial or production meaning.

## Always-visible non-occurrence result

The hub shows one always-visible non-occurrence result with the exact token `SYNTHETIC_NO_DEAL_EXECUTED` and the exact line `NO MATCH, SCORE, CONFIDENCE, QUALIFICATION, RISK, RANKING, CONTACT, DEAL OR OUTCOME OCCURRED`.

This result is bound to the current render only. It is not a ledger, not a persistent record, not an at-rest store and not reusable evidence, and it must never be described or treated as any of these. It stays visible at all times and for every anchor navigation back to the hub.

## Interaction, state, accessibility and presentation

Exactly eight same-tab native anchors are the only interactive elements. There are zero buttons, forms, editable inputs, selectors or custom widgets. There is no `useState`, no event handler, no `aria-live` region, no timer, no randomness, no motion, no persistence, no storage, no cookies, no cache, no logging, no diagnostics, no telemetry and no network, API, database, event or router call. Native link keyboard and focus semantics only are used, and focus must remain visibly discernible.

There is no mutable state of any kind. Nothing transitions, hides, reorders or replaces any region, and no region is ever hidden or advanced automatically. All regions and all eight entries are visible at once on initial render and on reload. The page exposes accessible `main`, `h1`, `h2`, ordered-list, list-item and anchor semantics.

The future code scope includes presentation-ready, responsive, hub-local styling with readable hierarchy, sufficient contrast, visible focus and reflow without horizontal page scrolling at supported viewports. Styling must not change G7–G15 or production styles. Motion and animation are prohibited, and reduced motion is trivially satisfied because none exists.

## Frozen vocabulary and boundaries

The frozen closed abstract data vocabulary is exactly: `SYNTHETIC_TEST_DEAL_A`, `SYNTHETIC_CAMPAIGN_PREVIEW_A`, `SYNTHETIC_FIXTURE_PARTY_A`, `SYNTHETIC_FIXTURE_PARTY_B`, `SYNTHETIC_SAFE_PRESENTATION_A`, `SYNTHETIC_HEADING`, `SYNTHETIC_MESSAGE`, `SYNTHETIC_NO_DEAL_EXECUTED`. No additional data-bearing token is authorized, no new scenario token may be introduced, and no `SCENARIO` line is authorized. New structural, accessibility and warning copy is permitted only where its exact value is frozen in the authorization record and allowlist, including the title `SYNTHETIC DEMO JOURNEY HUB — NOT PRODUCTION APPROVED`, the line `MANUAL DEV-ONLY HUB — NOT AN APPLICATION MENU`, the region title `SYNTHETIC DEMO JOURNEY — EIGHT MANUAL DEV-ONLY PAGES`, the label pattern `ENTRY n OF 8`, the region title `SAFE PRESENTATION — UNCHANGED G7/G8 REUSE`, the chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION`, the boundary panel `WHAT THIS HUB IS NOT — FIXTURE COPY`, `NOT A MATCH`, `NOT A SCORE OR CONFIDENCE`, `NOT A QUALIFICATION OR RISK DECISION`, `NOT A RANKING OR RECOMMENDATION`, `NOT A CONTACT, CAMPAIGN LAUNCH, DEAL OR OUTCOME`, `NOT PRODUCTION APPROVED` and `FIXTURE COPY — NOT A STATUS CLAIM`, and the region title `NON-OCCURRENCE RESULT`. No token or copy may resemble a plausible person, organization, address, geography, property, money amount, date, UUID, contact detail or business record.

The hub reuses the existing G7 Safe Presentation component unchanged and passes it the existing G8 synthetic input unchanged; it does not import or embed any G9, G10, G11, G12, G13, G14 or G15 page component. It links to the eight existing G8–G15 pages by exact same-tab native anchors only. G7 blocked/no-echo behavior and exact blocked output `SYNTHETIC PRESENTATION BLOCKED` remain unchanged, and G9, G10, G11, G12, G13 and G14 token, order, meaning and verification semantics remain unchanged. The hub cannot convert those artifacts into production evidence or approval.

Matching, candidate generation, comparison, compatibility, scoring, confidence, qualification, risk, ranking, thresholds, eligibility, recommendation, contact, communication, campaign launch, deal execution and outcome recording are prohibited. Also prohibited: network, API, database, event or router integration; persistence, storage, cookies, cache, logging, diagnostics or telemetry; any ledger or persistent treatment of the non-occurrence result; menu or default production-build exposure; forms, editable inputs, selectors or buttons; query/hash behavior; autoplay, timers or randomness; custom keyboard handlers; motion or animation; external URLs, `target="_blank"`, downloads or script URLs; and workflow execution or business computation.

The page has no menu entry, application link, router registration or production entry point and remains excluded from the default production build. Changes to `App.tsx`, `main.tsx`, `index.html`, Vite configuration, package manifests, lockfiles or API code are prohibited. No file outside the frozen nine-path allowlist may be created or modified.

## Required future verification

The future bounded test and manual browser smoke must prove, at minimum:

- focused tests: the `apps/web/tests/syntheticDemoJourneyHub.test.ts` suite covering the frozen copy, the eight frozen abstract tokens with no new scenario token and no `SCENARIO` line, the eight entries in exact order with exact titles, exact dev paths and exact same-tab native anchors, all-at-once presentation with no stepper or progression affordance, the six boundary panel rows, the chain warning, the unchanged G7/G8 reuse bytes and rendered output, anchor-only interaction with no buttons, forms, `useState`, event handlers or `aria-live`, the closed nine-path allowlist and production-root isolation;
- full tests and typecheck: the complete `apps/web` test suite and `npm --prefix apps/web run typecheck` pass with zero failures and no new diagnostics;
- closed scope and hashes: the closed nine-path boundary, no change outside it, and the frozen exact-byte LF-only allowlist SHA-256 `f7f552ce0487e9a961ee52b83f2ef8c78f4bcc3067add8a239f80fab6ec82a74` unchanged;
- responsive layout at exactly 360, 390, 768 and 1280 px with every region and all eight entries simultaneously visible and no horizontal page scrolling;
- native link keyboard and accessibility: exactly eight same-tab native anchors as the only interactive elements, native `Enter` link activation, visible focus, accessible `main`/`h1`/`h2`/ordered-list/list-item/anchor semantics, and no custom keyboard handlers;
- browser smoke: at the exact dev-only URL with all regions shown at once, all eight anchors resolving to the exact existing local dev paths in the same tab, and no page-level console errors or warnings;
- production isolation: no application, menu, router or default production-build integration and no hub copy in the production app;
- G15 unchanged: G15 and every prior G7–G14 file, behavior, token and verification meaning remain byte-for-byte and semantically unchanged.

The sole future success label is `G16_SYNTHETIC_DEMO_JOURNEY_HUB_VERIFIED`. It is a package result only, not a governance gate. It grants no Policy, Data Contracts, carrier, manifest, evidence, production, runtime, implementation, build or deployment approval.

## Governance

The five functions `PRODUCT`, `LEGAL`, `Chief AI Architect`, `AI` and `DEVELOPMENT` approve the same documentation boundary, package version `1.0`, baseline commit `9408fbda474ecdb61936956b1f01f9c0d1fa0ec3`, and frozen exact-byte LF-only allowlist SHA-256 `f7f552ce0487e9a961ee52b83f2ef8c78f4bcc3067add8a239f80fab6ec82a74`. `SECURITY/DLP` provides evidence only and has no approval authority.

`IMPLEMENTATION_READINESS_GATE`, `SYNTHETIC_ACCEPTANCE_GATE` and `PRODUCTION_LAUNCH_GATE` all remain `BLOCKED`; gate impact is `NONE`.
