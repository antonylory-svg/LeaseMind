# LeaseMind Matching G16 Synthetic Demo Journey Hub Authorization v1.0

**Artifact class:** non-canonical package authorization
**Authorization date:** `2026-09-24`
**Package version:** `1.0`
**Baseline branch:** `development/sprint-7-matching-g16-synthetic-demo-journey-hub`
**Baseline commit:** `9408fbda474ecdb61936956b1f01f9c0d1fa0ec3`
**Frozen file allowlist:** `05_DEVELOPMENT/matching-engine/synthetic-demo-journey-hub/G16_FILE_ALLOWLIST_v1.0.json` (LF-only line endings, UTF-8 no BOM)
**Frozen allowlist SHA-256:** `f7f552ce0487e9a961ee52b83f2ef8c78f4bcc3067add8a239f80fab6ec82a74` (exact bytes of the LF-only file)
**Authorization status:** `DOCUMENTATION_ONLY_AUTHORIZED`
**Code phase status:** `BLOCKED_PENDING_INDEPENDENT_AUDIT_AND_HUMAN_CONFIRMATION`

## 1. Authorization boundary

This record authorizes only the documentation phase for one isolated, manually opened, development-only responsive all-at-once synthetic demo journey hub. It freezes package version `1.0`, the exact baseline, the exact-byte allowlist hash, the complete nine-path file boundary, the all-at-once region presentation with its always-visible chain warning, the eight frozen existing G8–G15 page entries with their exact titles, exact local dev paths and same-tab native anchors, the unchanged G7 Safe Presentation reuse of the unchanged G8 input, the re-expressed unchanged G15 boundary and non-occurrence semantics, and the responsive presentation boundary. It does not authorize creation or modification of any path marked `planned`.

The code phase remains blocked until these three documentation artifacts pass an independent audit and a subsequent explicit human confirmation authorizes the code phase. Documentation authorship, silence, a package result label or any function approval below cannot substitute for either prerequisite.

This package is non-canonical. It creates no `XFR` ID, changes no canonical decision or identity count, requires no Inventory change, and does not amend or supersede any existing decision. All G7, G8, G9, G10, G11, G12, G13, G14 and G15 files, behavior, tokens and verification meaning must remain byte-for-byte and semantically unchanged.

## 2. Authorized hub

The future hub is a separate sibling page, manually opened in development only at exactly:

`http://127.0.0.1:5173/synthetic-demo-journey-hub.html`

Its HTML title and always-visible page title must be exactly:

`SYNTHETIC DEMO JOURNEY HUB — NOT PRODUCTION APPROVED`

The other always-visible line must be exactly:

`MANUAL DEV-ONLY HUB — NOT AN APPLICATION MENU`

The hub presents one always-visible synthetic demo journey: every region shown simultaneously, the eight frozen existing G8–G15 manual dev-only pages listed in fixed order as entries, the unchanged G7 Safe Presentation rendering of the unchanged G8 input, the re-expressed G15 boundary panel, and one always-visible non-occurrence result. Its only interactive elements are exactly eight same-tab native anchors. It has no `NEXT`, no `BACK`, no stepper, no current-region hiding and no progression.

## 3. Hub meaning

`hub` denotes an accessible navigation-aid page layout only. It is a product-shaped responsive arrangement of always-visible regions on one page, and it explicitly does not denote an application menu, production entry point, geography, tenancy, deployment, segmentation or routing. No application-menu, production-entry-point, geography, tenancy, deployment, segmentation or routing meaning, behavior or data may be introduced, implied or inferred from the layout. The hub remains a single synthetic demo page and is never an application menu, never a production menu, never a segmented, multi-tenant, multi-region, multi-deployment or routed surface, never a stepper and never a progression.

## 4. Frozen all-at-once regions and frozen eight-page journey entries

The hub shows all regions at the same time and is never a stepper. It links to the eight existing G8–G15 manual dev-only pages and reuses the unchanged G7 rendering of the unchanged G8 input, and it embeds no G9, G10, G11, G12, G13, G14 or G15 page component. The always-visible chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION` must remain visible with the journey region. The regions are:

- `SYNTHETIC DEMO JOURNEY — EIGHT MANUAL DEV-ONLY PAGES` — the journey region with exactly eight entries in fixed order and the adjacent chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION`;
- `SAFE PRESENTATION — UNCHANGED G7/G8 REUSE` — the unchanged G7 Safe Presentation rendering of the unchanged G8 input;
- `WHAT THIS HUB IS NOT — FIXTURE COPY` — the boundary panel with the always-adjacent line `FIXTURE COPY — NOT A STATUS CLAIM`;
- `NON-OCCURRENCE RESULT` — the always-visible non-occurrence result.

Each of the eight entries must be simultaneously visible and must carry, in fixed order, its exact `ENTRY n OF 8` label, its exact frozen existing page title, its exact visible local dev path, and exactly one same-tab native anchor whose `href` is exactly that exact existing local path. The order, titles and local dev paths are:

1. `ENTRY 1 OF 8` — `SYNTHETIC SAFE PRESENTATION DEMO — NOT PRODUCTION APPROVED` — `/safe-presentation-synthetic-demo.html`;
2. `ENTRY 2 OF 8` — `SYNTHETIC DEAL STORY DEMO — NOT PRODUCTION APPROVED` — `/synthetic-deal-story-demo.html`;
3. `ENTRY 3 OF 8` — `SYNTHETIC DEAL STORY WALKTHROUGH — NOT PRODUCTION APPROVED` — `/synthetic-deal-story-walkthrough.html`;
4. `ENTRY 4 OF 8` — `SYNTHETIC DEAL STORY REHEARSAL — NOT PRODUCTION APPROVED` — `/synthetic-deal-story-rehearsal.html`;
5. `ENTRY 5 OF 8` — `SYNTHETIC TEST-DEAL FLOW HARNESS — NOT PRODUCTION APPROVED` — `/synthetic-test-deal-flow-harness.html`;
6. `ENTRY 6 OF 8` — `SYNTHETIC MATCHING FLOW CONSOLE — NOT PRODUCTION APPROVED` — `/synthetic-matching-flow-console.html`;
7. `ENTRY 7 OF 8` — `SYNTHETIC MATCH REVIEW WORKSPACE — NOT PRODUCTION APPROVED` — `/synthetic-match-review-workspace.html`;
8. `ENTRY 8 OF 8` — `SYNTHETIC DEAL REHEARSAL BOARD — NOT PRODUCTION APPROVED` — `/synthetic-deal-rehearsal-board.html`.

The eight titles and local dev paths reuse the exact bytes frozen in the G8–G15 allowlists and page HTML. G7 is foundation only and is not a ninth entry; the entry count is exactly eight. No external URL, `target="_blank"`, `download`, query, hash, script URL, dynamic href, router, production menu or production entry point is authorized. Every visible evidence or reason row is exact pre-authored fixture copy reusing only existing abstract tokens and must carry the always-adjacent exact label `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`. No computed evidence and no recommendation is permitted.

## 5. Frozen boundary panel copy

The boundary panel title must be exactly `WHAT THIS HUB IS NOT — FIXTURE COPY`, with the always-adjacent exact line `FIXTURE COPY — NOT A STATUS CLAIM`. Its rows must be exactly the six re-expressed G15 boundary rows:

- `NOT A MATCH`;
- `NOT A SCORE OR CONFIDENCE`;
- `NOT A QUALIFICATION OR RISK DECISION`;
- `NOT A RANKING OR RECOMMENDATION`;
- `NOT A CONTACT, CAMPAIGN LAUNCH, DEAL OR OUTCOME`;
- `NOT PRODUCTION APPROVED`.

The panel is fixture copy only. It is a status disclaimer and not a status claim, and it grants no business, legal, financial or production meaning.

## 6. Frozen interaction, state and accessibility contract

The only interactive elements are exactly eight same-tab native anchors, one per journey entry. No button, form, editable input, selector or custom widget is authorized, and no progression control of any kind is authorized. Only native link keyboard and focus semantics are used: `Enter` activates a link, default browser focus must remain visibly discernible, and custom keyboard handlers are prohibited. No `aria-live` region is authorized.

There is no mutable state of any kind. No `useState`, no event handler, no timer, no randomness, no motion and no persistence are authorized. Nothing transitions, hides, reorders or replaces any region, and all regions and all eight entries remain visible at once on initial render and on reload. Query and hash state are prohibited.

The future page must use presentation-ready, responsive, hub-local styling: readable hierarchy, sufficient contrast, visible focus, and reflow without horizontal page scrolling at supported viewports. It cannot amend G7–G15 or production styles. Motion and animation are prohibited. Reduced motion is trivially satisfied because none exists.

## 7. Required unchanged reuse

The hub reuses the existing G7 Safe Presentation component unchanged and passes it the existing G8 input unchanged. G7 blocked/no-echo behavior and the exact blocked output `SYNTHETIC PRESENTATION BLOCKED` remain unchanged. The hub links to the eight existing G8–G15 manual dev-only pages by exact same-tab native anchors only and reuses their exact frozen page titles and local dev paths. It embeds no G9, G10, G11, G12, G13, G14 or G15 page component. G15 boundary rows and non-occurrence semantics are re-expressed unchanged. G16 is a separate sibling package and cannot reinterpret any prior package result as approval, evidence or production applicability.

## 8. Explicit exclusions and non-authorization

The following remain `OPEN`, excluded and unimplemented:

- actual Matching, candidate generation, comparison, compatibility or pair selection;
- Scoring, Confidence, Qualification, Risk, ranking, thresholds, eligibility or inference;
- recommendation, best-match or best-candidate claims;
- contact, communication, Campaign launch or routing;
- deal execution, transaction state, outcome recording, or legal or financial action;
- Safe Presentation Policy approval;
- Data Contracts, carrier or Controlled Artifact Manifest treatment;
- actual evidence, production data or production applicability;
- runtime, production build, deployment, API, database, event, storage, cache, logging, diagnostics or telemetry behavior;
- any application menu, production menu or production entry point.

This authorization grants no Policy, Data Contracts, dataset, evidence, carrier, manifest, production-data, runtime, implementation, build or deployment approval. The words `journey`, `hub`, `entry`, `page`, `navigation` and `evidence` are UI navigation and rehearsal labels only and grant no business, geographic, tenancy, deployment, segmentation, routing or production meaning.

## 9. Production isolation and frozen file boundary

The package must contain no organization, person, address, geography, contact detail, property category, money, date, UUID or plausible business record. Its closed abstract data vocabulary is exactly the `frozen_data_vocabulary` array of the allowlist: `SYNTHETIC_TEST_DEAL_A`, `SYNTHETIC_CAMPAIGN_PREVIEW_A`, `SYNTHETIC_FIXTURE_PARTY_A`, `SYNTHETIC_FIXTURE_PARTY_B`, `SYNTHETIC_SAFE_PRESENTATION_A`, `SYNTHETIC_HEADING`, `SYNTHETIC_MESSAGE`, `SYNTHETIC_NO_DEAL_EXECUTED`; no additional data-bearing token is authorized, no new scenario token may be introduced, and no `SCENARIO` line is authorized. New structural, accessibility and warning copy is permitted only where its exact value is frozen elsewhere in this record and the allowlist, including the title `SYNTHETIC DEMO JOURNEY HUB — NOT PRODUCTION APPROVED`, the line `MANUAL DEV-ONLY HUB — NOT AN APPLICATION MENU`, the journey region title `SYNTHETIC DEMO JOURNEY — EIGHT MANUAL DEV-ONLY PAGES`, the label pattern `ENTRY n OF 8`, the region title `SAFE PRESENTATION — UNCHANGED G7/G8 REUSE`, the chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION`, the boundary panel `WHAT THIS HUB IS NOT — FIXTURE COPY` with rows `NOT A MATCH`, `NOT A SCORE OR CONFIDENCE`, `NOT A QUALIFICATION OR RISK DECISION`, `NOT A RANKING OR RECOMMENDATION`, `NOT A CONTACT, CAMPAIGN LAUNCH, DEAL OR OUTCOME`, `NOT PRODUCTION APPROVED` and adjacent line `FIXTURE COPY — NOT A STATUS CLAIM`, and the region title `NON-OCCURRENCE RESULT`.

It must introduce no forms, editable inputs, selectors, buttons, widgets, actual business data, query/hash behavior, autoplay, timers, randomness, custom keyboard handling, network calls, persistence, storage, cookies, cache, logging, diagnostics or telemetry. The non-occurrence result must never be treated as a ledger or as persistent. No runtime, workflow or production integration is authorized.

The page must have no menu entry, application link or router registration and must remain excluded from the default production build. Changes to G7–G15; `App.tsx`; `main.tsx`; `index.html`; Vite configuration; package manifests; lockfiles; API code/configuration; or production route, build or deployment configuration are prohibited.

The sole authoritative path set is the closed `allowed_paths` array in `G16_FILE_ALLOWLIST_v1.0.json`, bound to package version `1.0`, baseline commit `9408fbda474ecdb61936956b1f01f9c0d1fa0ec3`, and exact-byte LF-only SHA-256 `f7f552ce0487e9a961ee52b83f2ef8c78f4bcc3067add8a239f80fab6ec82a74`.

Exactly these three documentation artifacts are `present` in this phase: this authorization record, the package README and the frozen allowlist. Exactly six paths are `planned`: the standalone HTML page, the frozen scenario module, the isolated hub component, the isolated entry module, the bounded test and the verification result. They must remain absent until the audit and confirmation prerequisite is satisfied. The allowlist omits its own hash to avoid self-reference; this record and the README bind its final bytes.

## 10. Required bounded verification

A separately authorized future code package must verify at minimum:

- the closed nine-path boundary and the absence of changes outside it;
- focused tests: the bounded `syntheticDemoJourneyHub.test.ts` suite covering frozen copy, the eight frozen abstract tokens with no new scenario token and no `SCENARIO` line, the eight entries in exact order with exact titles, exact local dev paths and exact same-tab native anchors, all-at-once presentation, the six boundary panel rows, the chain warning, unchanged G7/G8 reuse bytes and rendered output, anchor-only interaction, prohibited-behavior absence, the closed nine-path allowlist and production-root isolation;
- the full `apps/web` test suite and `npm --prefix apps/web run typecheck` passing with zero failures and no new diagnostics;
- all regions and all eight entries simultaneously visible on initial render and reload;
- the absence of any `NEXT`, `BACK`, stepper, current-region hiding, progression or automatic transition;
- exact simultaneous region copy and fixed layout order, with the chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION` always visible;
- every visible evidence or reason row being exact pre-authored fixture copy carrying the always-adjacent exact label `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`, with no computed evidence and no recommendation;
- exactly eight same-tab native anchors as the only interactive elements, each with the exact accessible name of its entry, visible focus and native `Enter` activation, with no `aria-live`, no `useState`, no event handler and no custom keyboard code;
- the boundary panel rows and the adjacent line `FIXTURE COPY — NOT A STATUS CLAIM` remaining visible;
- unchanged G7 rendering of the unchanged G8 input and unchanged G9–G15 semantics, with no G9–G15 page component embedded;
- presentation-ready responsive reflow at exactly 360, 390, 768 and 1280 px, sufficient contrast, supported-viewport readability and no horizontal page scrolling;
- the absence of forbidden behavior, business computation, network request, external URL, `target="_blank"`, download, dynamic href, router, persistence, storage, logging, diagnostics, telemetry or motion;
- manual or automated browser smoke at the exact dev-only URL without application, router, menu or default production-build integration;
- G15 and all prior G7–G14 files, behavior, tokens and verification meaning remaining byte-for-byte and semantically unchanged.

## 11. Verification and non-conflation

If the separately authorized future code package passes bounded verification, its sole package result label is:

`G16_SYNTHETIC_DEMO_JOURNEY_HUB_VERIFIED`

This is a package verification result only and not a governance gate. It does not mean a match, comparison, score, confidence, Qualification, Risk decision, ranking, threshold, eligibility, recommendation, contact, Campaign launch, deal, outcome, approval, workflow execution or production operation occurred.

## 12. Approval matrix

| Function | Decision | Exact scope |
| --- | --- | --- |
| `PRODUCT` | `APPROVED` | Product-shaped all-at-once dev-only journey hub listing the eight existing G8–G15 pages, the boundary panel and the non-occurrence result |
| `LEGAL` | `APPROVED` | Synthetic-only closed abstract vocabulary; no person, contact, asset, transaction, legal or financial action |
| `Chief AI Architect` | `APPROVED` | Unchanged G7–G15 semantics, non-conflation and the closed nine-path boundary |
| `AI` | `APPROVED` | Pre-authored fixture-only copy and exact prohibitions on matching, comparison, scoring, confidence, qualification, risk, ranking, eligibility and recommendation |
| `DEVELOPMENT` | `APPROVED` | Exact baseline, all-at-once layout contract, anchor-only interaction, responsive accessibility, bounded verification and production isolation |

No approval above extends to SECURITY or DLP, and no approval authorizes policy activation, data contracts, carrier or manifest treatment, real evidence, production data, runtime code, deployment or gate changes.

## 13. Gate state

Gate impact is `NONE`. All three governance gates remain unchanged:

- `IMPLEMENTATION_READINESS_GATE`: `BLOCKED`;
- `SYNTHETIC_ACCEPTANCE_GATE`: `BLOCKED`;
- `PRODUCTION_LAUNCH_GATE`: `BLOCKED`.

No result label, fixture, anchor, journey entry or documentation artifact may advance, soften, reinterpret or satisfy any gate.

## 14. Authorization conclusion

`G16 SYNTHETIC DEMO JOURNEY HUB DOCUMENTATION PACKAGE AUTHORIZED — CODE FILE CREATION REQUIRES INDEPENDENT AUDIT AND SUBSEQUENT EXPLICIT HUMAN CONFIRMATION; G7/G8/G9/G10/G11/G12/G13/G14/G15 HISTORY, MATCHING, COMPARISON, SCORING, CONFIDENCE, QUALIFICATION, RISK, RANKING, ELIGIBILITY, RECOMMENDATION, CONTACT, CAMPAIGN, DEAL, OUTCOME, WORKFLOW EXECUTION, POLICY, DATA CONTRACTS, CARRIER, MANIFEST, EVIDENCE, PRODUCTION, RUNTIME, IMPLEMENTATION AND ALL THREE GATES REMAIN OPEN/BLOCKED.`
