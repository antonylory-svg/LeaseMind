# LeaseMind Matching G15 Synthetic Deal Rehearsal Board Authorization v1.0

**Artifact class:** non-canonical package authorization
**Authorization date:** `2026-09-24`
**Package version:** `1.0`
**Baseline branch:** `development/sprint-7-matching-g15-synthetic-deal-rehearsal-board`
**Baseline commit:** `0b3d965c29cad04ed67a73545f8c22d054da115d`
**Frozen file allowlist:** `05_DEVELOPMENT/matching-engine/synthetic-deal-rehearsal-board/G15_FILE_ALLOWLIST_v1.0.json` (LF-only line endings, UTF-8 no BOM)
**Frozen allowlist SHA-256:** `53addc8cb9c13a6fece1f8cc85b44cb332f86f56ccaa93d16ce3725c031651a2` (exact bytes of the LF-only file)
**Authorization status:** `DOCUMENTATION_ONLY_AUTHORIZED`
**Code phase status:** `BLOCKED_PENDING_INDEPENDENT_AUDIT_AND_HUMAN_CONFIRMATION`

## 1. Authorization boundary

This record authorizes only the documentation phase for one isolated, manually opened, development-only responsive product-shaped single-page synthetic deal rehearsal board. It freezes package version `1.0`, the exact baseline, the exact-byte allowlist hash, the complete nine-path file boundary, the all-at-once region presentation with its always-visible chain warning, the unchanged G7 Safe Presentation reuse of the unchanged G8 input, the re-expressed unchanged G13/G14 token, order, meaning and non-occurrence semantics, the boundary panel, the three human rehearsal controls and the responsive presentation boundary. It does not authorize creation or modification of any path marked `planned`.

The code phase remains blocked until these three documentation artifacts pass an independent audit and a subsequent explicit human confirmation authorizes the code phase. Documentation authorship, silence, a package result label or any function approval below cannot substitute for either prerequisite.

This package is non-canonical. It creates no `XFR` ID, changes no canonical decision or identity count, requires no Inventory change, and does not amend or supersede any existing decision. All G7, G8, G9, G10, G11, G12, G13 and G14 files, behavior, tokens and verification meaning must remain byte-for-byte and semantically unchanged.

## 2. Authorized board

The future board is a separate sibling page, manually opened in development only at exactly:

`http://127.0.0.1:5173/synthetic-deal-rehearsal-board.html`

Its HTML title and always-visible page title must be exactly:

`SYNTHETIC DEAL REHEARSAL BOARD — NOT PRODUCTION APPROVED`

The other always-visible line must be exactly:

`MANUAL DEV-ONLY REHEARSAL — NO DEAL EXECUTION`

The board presents a single product-shaped end-to-end rehearsal: every region shown simultaneously, the unchanged G7 Safe Presentation rendering of the unchanged G8 input, the re-expressed G13 six-node tokens in fixed order, the re-expressed G14 two-sided co-display, the boundary panel, one always-visible non-occurrence result and exactly three human rehearsal controls. It has no `NEXT`, no `BACK`, no stepper, no current-region hiding and no progression.

## 3. Board meaning

`board` denotes an accessible page layout only. It is a product-shaped responsive arrangement of always-visible regions on one page, and it explicitly does not denote geography, tenancy, deployment, segmentation or routing. No geography, tenancy, deployment, segmentation or routing meaning, behavior or data may be introduced, implied or inferred from the layout. The `OWNER-SIDE NEED` and `TENANT-SIDE CANDIDATE` labels are UI rehearsal labels only and grant no business, property, money, contact or legal meaning. The board remains a single synthetic rehearsal page and is never a segmented, multi-tenant, multi-region, multi-deployment or routed surface, never a stepper and never a progression.

## 4. Frozen all-at-once regions and re-expressed G13/G14 copy

The board shows all regions at the same time and is never a stepper. It re-expresses the G13 six-node sequence and the G14 two-sided co-display, and it embeds no G11, G12, G13 or G14 page component. The always-visible chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION` must remain visible with the chain. The regions are:

- the two-sided co-display `OWNER-SIDE NEED AND TENANT-SIDE CANDIDATE` with `OWNER-SIDE NEED — SYNTHETIC_FIXTURE_PARTY_A`, `TENANT-SIDE CANDIDATE — SYNTHETIC_FIXTURE_PARTY_B`, the always-adjacent warning `CO-DISPLAY ONLY — NOT COMPARED OR MATCHED` and the pair warning `FIXTURE ONLY — NOT MATCHED OR RANKED`;
- the `END-TO-END REHEARSAL CHAIN — SHOWN AT ONCE` region carrying the six preserved G13 nodes in fixed order;
- the `PRE-AUTHORED SYNTHETIC FIXTURE` region with the always-adjacent label `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`;
- the `SAFE PRESENTATION` region reusing the unchanged G7 rendering of the unchanged G8 input;
- the `WHAT THIS BOARD IS NOT` boundary panel;
- the `NON-OCCURRENCE RESULT` region.

The preserved G13 six-node sequence, unchanged in token, order and meaning, is:

1. `SYNTHETIC_TEST_DEAL_A` — shown as a pre-authored fixture evidence row;
2. `SYNTHETIC_CAMPAIGN_PREVIEW_A` — shown as a pre-authored fixture evidence row;
3. `SYNTHETIC_FIXTURE_PARTY_A` + `SYNTHETIC_FIXTURE_PARTY_B` — shown as the two-sided co-display;
4. `SYNTHETIC_SAFE_PRESENTATION_A` (with `SYNTHETIC_HEADING` and `SYNTHETIC_MESSAGE`) — shown via unchanged G7 rendering of unchanged G8 input;
5. `HUMAN REHEARSAL DISPOSITION REQUIRED` — expressed only by the human rehearsal controls;
6. `SYNTHETIC_NO_DEAL_EXECUTED` — shown as the always-visible non-occurrence result.

Every visible evidence or reason row is exact pre-authored fixture copy reusing only existing abstract tokens and must carry the always-adjacent exact label `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`. No computed evidence and no recommendation is permitted. The two-sided co-display is co-display only; no comparison, compatibility, matching, scoring, confidence, ranking, eligibility or recommendation may be produced, shown or implied.

## 5. Frozen boundary panel copy

The boundary panel title must be exactly `WHAT THIS BOARD IS NOT — FIXTURE COPY`, with the always-adjacent exact line `FIXTURE COPY — NOT A STATUS CLAIM`. Its rows must be exactly:

- `NOT A MATCH`;
- `NOT A SCORE OR CONFIDENCE`;
- `NOT A QUALIFICATION OR RISK DECISION`;
- `NOT A RANKING OR RECOMMENDATION`;
- `NOT A CONTACT, CAMPAIGN LAUNCH, DEAL OR OUTCOME`;
- `NOT PRODUCTION APPROVED`.

The panel is fixture copy only. It is a status disclaimer and not a status claim, and it grants no business, legal, financial or production meaning.

## 6. Frozen interaction, state and accessibility contract

The only authorized controls are native `type="button"` elements: `REHEARSE INTEREST`, `REHEARSE HOLD` and `RESET`, in exactly that DOM order. No progression control of any kind is authorized. The controls are contained in navigation whose `aria-label` is exactly `SYNTHETIC DEAL REHEARSAL BOARD CONTROLS`. The rehearsal controls are grouped with accessible name exactly `HUMAN REHEARSAL DISPOSITION`. The selection-status region has `aria-live="polite"` and `aria-atomic="true"`. Default browser keyboard focus must remain visibly discernible. Only native `Enter` and `Space` button activation is used; custom keyboard handlers are prohibited.

The only mutable state is transient, in-memory and non-persistent: the in-memory `REHEARSE INTEREST` / `REHEARSE HOLD` selection and the `RESET` action that clears it. Either selection must visibly state `UI REHEARSAL SELECTION ONLY — NOT APPROVAL`. Neither selection transitions, hides, reorders or replaces any region, and both selections preserve the same always-visible `SYNTHETIC_NO_DEAL_EXECUTED` and the exact non-occurrence line `NO MATCH, SCORE, CONFIDENCE, QUALIFICATION, RISK, RANKING, CONTACT, DEAL OR OUTCOME OCCURRED`. `RESET` clears the selection, and a reload also clears it because no state is persisted.

The future page must use presentation-ready, responsive, board-local styling: readable hierarchy, sufficient contrast, visible focus, and reflow without horizontal page scrolling at supported viewports. It cannot amend G7–G14 or production styles. Motion and animation are prohibited. Reduced motion is trivially satisfied because none exists.

## 7. Required unchanged reuse

The board reuses the existing G7 Safe Presentation component unchanged and passes it the existing G8 input unchanged. G7 blocked/no-echo behavior and the exact blocked output `SYNTHETIC PRESENTATION BLOCKED` remain unchanged. G13 and G14 six-node/two-sided tokens, order, meaning and non-occurrence semantics are re-expressed unchanged, and the board is never converted into a stepper. G9, G10, G11 and G12 token, order, meaning and verification semantics remain unchanged. The board embeds no G11, G12, G13 or G14 page component. G15 is a separate sibling package and cannot reinterpret any prior package result as approval, evidence or production applicability.

## 8. Explicit exclusions and non-authorization

The following remain `OPEN`, excluded and unimplemented:

- actual Matching, candidate generation, comparison, compatibility or pair selection;
- Scoring, Confidence, Qualification, Risk, ranking, thresholds, eligibility or inference;
- recommendation, best-match or best-candidate claims;
- contact, communication, Campaign launch or routing;
- deal execution, transaction state, outcome recording, or legal or financial action;
- Safe Presentation Policy approval;
- Data Contracts, carrier or Controlled Artifact Manifest treatment;
- actual evidence, production data or production applicability;
- runtime, production build, deployment, API, database, event, storage, cache, logging, diagnostics or telemetry behavior.

This authorization grants no Policy, Data Contracts, dataset, evidence, carrier, manifest, production-data, runtime, implementation, build or deployment approval. The words `owner`, `tenant`, `need`, `candidate`, `interest`, `hold`, `board`, `rehearsal`, `evidence` and `reason` are UI rehearsal labels only and grant no business, geographic, tenancy, deployment, segmentation or routing meaning.

## 9. Production isolation and frozen file boundary

The package must contain no organization, person, address, geography, contact detail, property category, money, date, UUID or plausible business record. Its closed abstract data vocabulary is exactly the `frozen_data_vocabulary` array of the allowlist: `SYNTHETIC_TEST_DEAL_A`, `SYNTHETIC_CAMPAIGN_PREVIEW_A`, `SYNTHETIC_FIXTURE_PARTY_A`, `SYNTHETIC_FIXTURE_PARTY_B`, `SYNTHETIC_SAFE_PRESENTATION_A`, `SYNTHETIC_HEADING`, `SYNTHETIC_MESSAGE`, `SYNTHETIC_NO_DEAL_EXECUTED`; no additional data-bearing token is authorized, no new scenario token may be introduced, and no `SCENARIO` line is authorized. New structural, accessibility, control and warning copy is permitted only where its exact value is frozen elsewhere in this record and the allowlist, including rehearsal controls `REHEARSE INTEREST`, `REHEARSE HOLD`, `RESET`, the chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION`, the boundary panel `WHAT THIS BOARD IS NOT — FIXTURE COPY` with rows `NOT A MATCH`, `NOT A SCORE OR CONFIDENCE`, `NOT A QUALIFICATION OR RISK DECISION`, `NOT A RANKING OR RECOMMENDATION`, `NOT A CONTACT, CAMPAIGN LAUNCH, DEAL OR OUTCOME`, `NOT PRODUCTION APPROVED` and `FIXTURE COPY — NOT A STATUS CLAIM`, and warnings `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`, `CO-DISPLAY ONLY — NOT COMPARED OR MATCHED`, `FIXTURE ONLY — NOT MATCHED OR RANKED` and `UI REHEARSAL SELECTION ONLY — NOT APPROVAL`.

It must introduce no forms, editable inputs, selectors, links, actual business data, query/hash behavior, autoplay, timers, randomness, custom keyboard handling, network calls, persistence, storage, cookies, cache, logging, diagnostics or telemetry. The non-occurrence result must never be treated as a ledger or as persistent. No runtime, workflow or production integration is authorized.

The page must have no menu entry, application link or router registration and must remain excluded from the default production build. Changes to G7–G14; `App.tsx`; `main.tsx`; `index.html`; Vite configuration; package manifests; lockfiles; API code/configuration; or production route, build or deployment configuration are prohibited.

The sole authoritative path set is the closed `allowed_paths` array in `G15_FILE_ALLOWLIST_v1.0.json`, bound to package version `1.0`, baseline commit `0b3d965c29cad04ed67a73545f8c22d054da115d`, and exact-byte LF-only SHA-256 `53addc8cb9c13a6fece1f8cc85b44cb332f86f56ccaa93d16ce3725c031651a2`.

Exactly these three documentation artifacts are `present` in this phase: this authorization record, the package README and the frozen allowlist. Exactly six paths are `planned`: the standalone HTML page, the frozen scenario module, the isolated board component, the isolated entry module, the bounded test and the verification result. They must remain absent until the audit and confirmation prerequisite is satisfied. The allowlist omits its own hash to avoid self-reference; this record and the README bind its final bytes.

## 10. Required bounded verification

A separately authorized future code package must verify at minimum:

- the closed nine-path boundary and the absence of changes outside it;
- focused tests: the bounded `syntheticDealRehearsalBoard.test.ts` suite covering frozen copy, the eight frozen abstract tokens, the re-expressed G13/G14 tile order, all-at-once presentation, the boundary panel rows, both co-display warnings, unchanged G7/G8 reuse bytes and rendered output, native button order and type, aria navigation/group/live region, transient-only selection, prohibited-behavior absence, the closed nine-path allowlist and production-root isolation;
- the full `apps/web` test suite and `npm --prefix apps/web run typecheck` passing with zero failures and no new diagnostics;
- all regions simultaneously visible on initial render, reload and `RESET`;
- the absence of any `NEXT`, `BACK`, stepper, current-region hiding, progression or automatic transition;
- exact simultaneous region copy and fixed layout order, with the chain warning `SEQUENCE SHOWN AT ONCE — NOT A STEPPER — NOT PROGRESSION` always visible;
- every visible evidence or reason row being exact pre-authored fixture copy carrying the always-adjacent exact label `PRE-AUTHORED SYNTHETIC FIXTURE — NOT COMPUTED`, with no computed evidence and no recommendation;
- the co-display warning `CO-DISPLAY ONLY — NOT COMPARED OR MATCHED` and the pair warning `FIXTURE ONLY — NOT MATCHED OR RANKED` remaining visible;
- the boundary panel rows and the adjacent line `FIXTURE COPY — NOT A STATUS CLAIM` remaining visible;
- unchanged G7 rendering of the unchanged G8 input and unchanged G9–G14 semantics, with no G11–G14 page component embedded;
- both rehearsal choices producing only transient in-memory UI state, visibly stating `UI REHEARSAL SELECTION ONLY — NOT APPROVAL`, and preserving the same `SYNTHETIC_NO_DEAL_EXECUTED`;
- `RESET` and reload clearing the selection;
- exact native-button DOM order, accessible names, live-region attributes, visible focus and native keyboard activation;
- presentation-ready responsive reflow at exactly 360, 390, 768 and 1280 px, sufficient contrast, supported-viewport readability and no horizontal page scrolling;
- the absence of forbidden behavior, business computation, network request, persistence, storage, logging, diagnostics, telemetry or motion;
- manual or automated browser smoke at the exact dev-only URL without application, router, menu or default production-build integration;
- G14 and all prior G7–G13 files, behavior, tokens and verification meaning remaining byte-for-byte and semantically unchanged.

## 11. Verification and non-conflation

If the separately authorized future code package passes bounded verification, its sole package result label is:

`G15_SYNTHETIC_DEAL_REHEARSAL_BOARD_VERIFIED`

This is a package verification result only and not a governance gate. It does not mean a match, comparison, score, confidence, Qualification, Risk decision, ranking, threshold, eligibility, recommendation, contact, Campaign launch, deal, outcome, approval, workflow execution or production operation occurred.

## 12. Approval matrix

| Function | Decision | Exact scope |
| --- | --- | --- |
| `PRODUCT` | `APPROVED` | Product-shaped single board with all regions shown at once, pre-authored fixture rows, the boundary panel and two UI-only rehearsal choices |
| `LEGAL` | `APPROVED` | Synthetic-only closed abstract vocabulary; no person, contact, asset, transaction, legal or financial action |
| `Chief AI Architect` | `APPROVED` | Unchanged G7–G14 semantics, non-conflation and the closed nine-path boundary |
| `AI` | `APPROVED` | Pre-authored fixture-only rows and exact prohibitions on matching, comparison, scoring, confidence, qualification, risk, ranking, eligibility and recommendation |
| `DEVELOPMENT` | `APPROVED` | Exact baseline, all-at-once layout contract, transient in-memory state, responsive accessibility, bounded verification and production isolation |

No approval above extends to SECURITY or DLP, and no approval authorizes policy activation, data contracts, carrier or manifest treatment, real evidence, production data, runtime code, deployment or gate changes.

## 13. Gate state

Gate impact is `NONE`. All three governance gates remain unchanged:

- `IMPLEMENTATION_READINESS_GATE`: `BLOCKED`;
- `SYNTHETIC_ACCEPTANCE_GATE`: `BLOCKED`;
- `PRODUCTION_LAUNCH_GATE`: `BLOCKED`.

No result label, fixture, co-display, rehearsal selection or documentation artifact may advance, soften, reinterpret or satisfy any gate.

## 14. Authorization conclusion

`G15 SYNTHETIC DEAL REHEARSAL BOARD DOCUMENTATION PACKAGE AUTHORIZED — CODE FILE CREATION REQUIRES INDEPENDENT AUDIT AND SUBSEQUENT EXPLICIT HUMAN CONFIRMATION; G7/G8/G9/G10/G11/G12/G13/G14 HISTORY, MATCHING, COMPARISON, SCORING, CONFIDENCE, QUALIFICATION, RISK, RANKING, ELIGIBILITY, RECOMMENDATION, CONTACT, CAMPAIGN, DEAL, OUTCOME, WORKFLOW EXECUTION, POLICY, DATA CONTRACTS, CARRIER, MANIFEST, EVIDENCE, PRODUCTION, RUNTIME, IMPLEMENTATION AND ALL THREE GATES REMAIN OPEN/BLOCKED.`
