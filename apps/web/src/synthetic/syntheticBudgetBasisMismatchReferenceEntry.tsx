import { createRoot } from 'react-dom/client';
import SyntheticBudgetBasisMismatchReference from './SyntheticBudgetBasisMismatchReference.js';

const root = document.getElementById('root');

if (root) {
  createRoot(root).render(<SyntheticBudgetBasisMismatchReference />);
}
