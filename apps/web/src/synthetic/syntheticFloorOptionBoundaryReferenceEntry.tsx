import { createRoot } from 'react-dom/client';
import SyntheticFloorOptionBoundaryReference from './SyntheticFloorOptionBoundaryReference.js';

const root = document.getElementById('root');

if (root) {
  createRoot(root).render(<SyntheticFloorOptionBoundaryReference />);
}
