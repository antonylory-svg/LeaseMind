import { createRoot } from 'react-dom/client';
import SyntheticEntranceRequirementCompatibilityReference from './SyntheticEntranceRequirementCompatibilityReference.js';

const root = document.getElementById('root');

if (root) {
  createRoot(root).render(<SyntheticEntranceRequirementCompatibilityReference />);
}
