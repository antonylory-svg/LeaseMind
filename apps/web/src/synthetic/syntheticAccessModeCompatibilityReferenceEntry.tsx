import { createRoot } from 'react-dom/client';
import SyntheticAccessModeCompatibilityReference from './SyntheticAccessModeCompatibilityReference.js';

const root = document.getElementById('root');

if (root) {
  createRoot(root).render(<SyntheticAccessModeCompatibilityReference />);
}
