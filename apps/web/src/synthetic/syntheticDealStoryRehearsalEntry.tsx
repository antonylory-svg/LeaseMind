import { createRoot } from 'react-dom/client';
import SyntheticDealStoryRehearsal from './SyntheticDealStoryRehearsal.js';

const root = document.getElementById('root');

if (root) {
  createRoot(root).render(<SyntheticDealStoryRehearsal />);
}
