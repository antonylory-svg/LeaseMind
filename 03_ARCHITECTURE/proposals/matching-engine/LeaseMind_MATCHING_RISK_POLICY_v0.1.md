# LeaseMind MATCHING_RISK_POLICY

**Версия:** 0.1
**Дата:** 2026-08-22
**Статус:** `Proposal for cross-functional review — does not authorize implementation`
**Artifact owner:** `Chief AI Architect + LEGAL` — `SOURCE_NORMATIVE`, Architecture §52 (Controlled Artifact Manifest, запись `MATCHING_RISK_POLICY`, «Qualification blocker»)
**Threshold decision owner (Architecture §37 вопрос №8, численные human-review thresholds):** `AI + LEGAL` — `SOURCE_NORMATIVE`; отдельная owner-грань, не тождественна artifact owner выше
**Lawful-sources decision owner (Architecture §37 вопрос №7):** `LEGAL` — `SOURCE_NORMATIVE`
**DEVELOPMENT review** для reproducibility/technical feasibility — `DECISION_CANDIDATE_FOR_REVIEW`, источник напрямую не назначает DEVELOPMENT единоличным owner'ом ни одного Risk-решения

**This proposal does not authorize implementation, model release, synthetic acceptance, production use, real personal data, automated policy promotion, or any gate.**

Документ не закрывает Architecture §37 вопрос №8 полностью: `XFR-D-M2` разрешает только `PARTIALLY_RESOLVED_BOUNDARY`, а exact trigger/threshold/mapping/route contents остаются `OPEN`; ни один gate не переводится в `READY`.

Human-approved `LeaseMind_MATCHING_DECISION_XFR-D-M4_v1.0.md` independently gives merged `EP-10 + MRP-11 + MQP-15 + MSP-14` only `PARTIALLY_RESOLVED_BOUNDARY`: all four source keys remain `PRIMARY_MERGED_MEMBER`; governance owner `DEVELOPMENT + AI` is candidate-derived and not `SOURCE_NORMATIVE`; mandatory approvers are `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical owner `DEVELOPMENT + AI` has no unilateral authority; Evaluation, Risk, Qualification and Scoring artifact owners remain separate under Architecture §52. Deterministic replay remains exact-only and severity-1 on mismatch; recorded and bounded probabilistic replay remain distinct; probabilistic output is advisory only. Every exact tolerance/invariant/data/statistical/policy/production/carrier/runtime/implementation content remains `OPEN`, and no gate is changed.

**Related replay decision:** `LeaseMind_MATCHING_DECISION_XFR-D-M4_v1.0.md`.

**Current merged row №10 overlay:** human-approved `XFR-D-M3 v1.0` gives `MRP-10` only `PARTIALLY_RESOLVED_BOUNDARY`; canonical `FS-07 + EP-09 + MRP-10 + SPP-04 → XFR-D-M3`, with all four source keys remaining `PRIMARY_MERGED_MEMBER`. Governance owner is `PRODUCT + LEGAL`, human-approved candidate-derived and not `SOURCE_NORMATIVE` for the whole merge; `DEVELOPMENT` is measurability-only. Artifact owners remain Feature `PRODUCT + LEGAL + AI`, Evaluation `AI + DEVELOPMENT`, Risk `Chief AI Architect + LEGAL`, Safe Presentation `PRODUCT + LEGAL`; mandatory approvers are `Chief AI Architect + AI + DEVELOPMENT`; evidence/technical-procedure owner is `AI + DEVELOPMENT` without unilateral authority; any future exact change requires all five functions on the same version/hash and immutable evidence package. Only role separation, four-context separation, closed version/hash-bound candidate discipline, affected-use fail-closed, non-compensation, source-normative deny preservation, historical binding, no-automatic-action and qualitative evidence prerequisites are approved. Dataset irreversible de-identification, Evaluation privacy/small-cell diagnostics, Risk signal and Safe Presentation evidence remain distinct with no cross-context authorization; pseudonymization is not anonymization; privacy small-cell is not statistical power/coverage; exact address, coordinates and high-risk identifying combinations remain denied. Exact method/cohort/quasi-ID/adversary/linkage/numerator/denominator/counting/threshold/comparator/tolerance/statistics/data/evidence/policy/production/schema/runtime/implementation remain `OPEN`; independent decisions and all three gates remain unchanged/`BLOCKED`.

Human-approved merged decision `LeaseMind_MATCHING_DECISION_XFR-D-M2_v1.0.md` gives `MRP-04 + MQP-09 → XFR-D-M2` only `PARTIALLY_RESOLVED_BOUNDARY`: substantive governance owner `AI + LEGAL` remains `SOURCE_NORMATIVE` from Architecture §37 question №8; Risk artifact owner remains `Chief AI Architect + LEGAL`; Qualification artifact owner remains `Chief AI Architect + PRODUCT` under `XFR-D-030`; mandatory approvers are `Chief AI Architect + PRODUCT + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT` has no unilateral authority. Approved are only qualitative governance/evidence safeguards; no trigger, threshold, mapping, route, policy, data, production, runtime, implementation or gate is approved.

Human-approved governance decisions `LeaseMind_MATCHING_DECISION_XFR-D-047_v1.0.md` (Risk output representation governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-048_v1.0.md` (Risk aggregation qualitative model and non-compensation boundary), `LeaseMind_MATCHING_DECISION_XFR-D-049_v1.0.md` (per-factor evidence-sufficiency governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-050_v1.0.md` (Risk calibration evidence-governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-051_v1.0.md` (missing/conflicting/stale operational-semantics governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-052_v1.0.md` (Risk reason-reference governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-053_v1.0.md` (Risk reviewer-authority/queue/Decision-linkage governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-054_v1.0.md` (protected/proxy classification and lawful-basis two-stage governance boundary), `LeaseMind_MATCHING_DECISION_XFR-D-055_v1.0.md` (Risk output → Qualification interface governance boundary) и `LeaseMind_MATCHING_DECISION_XFR-D-056_v1.0.md` (duplication-detection governance/authority boundary) обязательны для соответствующих status overlays §5/§7–§15 ниже. Их approval не переводит этот Proposal в `APPROVED`: `XFR-D-047`/`XFR-D-049`–`XFR-D-056` разрешают только `PARTIALLY_RESOLVED_BOUNDARY`; exact identifiers/enums/catalogs/values/order/severity/criticality/mappings/compatibility/fallback/TTL, trigger, entity taxonomy/keys, algorithms/thresholds/evidence, dataset/statistical/result contents, lawful classifications/determinations, reviewer appointments/RBAC/queue/SLA/outcomes/Decision schema/appeal, numeric content, runtime representation/carrier, policies и implementation остаются `OPEN`.

**Связанные документы:** `LeaseMind_MATCHING_ENGINE_ARCHITECTURE_v1.1.md`, `LeaseMind_MATCHING_DATA_CONTRACTS_v1.0.md` (контрактные/replay границы — проверено: Risk-специфичных names/enums, generic Risk-review queue/carrier и entity-duplication carrier там нет; dispute/previous-contact-specific decision structures не являются generic Risk-review authority; event/inbox/idempotency deduplication не является entity resolution), `LeaseMind_MATCHING_FEATURE_SCHEMA_v0.1.md` (Proposal-зависимость, source facts/evidence), `LeaseMind_MATCHING_EVALUATION_PLAN_v0.1.md` (Proposal-зависимость, dataset/metric/threshold-search procedure evidence, не owner финальных Risk thresholds), `LeaseMind_MATCHING_QUALIFICATION_POLICY_v0.1.md` (Proposal-зависимость, final routing boundary; `XFR-D-041` reviewer queue/authority остаётся distinct), `LeaseMind_MATCHING_DECISION_XFR-D-047_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-048_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-049_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-050_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-051_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-052_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-053_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-054_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-055_v1.0.md`, `LeaseMind_MATCHING_DECISION_XFR-D-056_v1.0.md`, `02_PRODUCT/CAMPAIGN_OUTCOMES.md`, `02_PRODUCT/ANALYSIS_SNAPSHOT.md`, `05_DEVELOPMENT/matching-engine/reviews/LeaseMind_DEVELOPMENT_REVIEW_MATCHING_ENGINE_v1.1_EIGHTH.md` (только DEVELOPMENT evidence, не источник новых архитектурных решений).

---

## 1. Metadata и нормативная дисциплина

Каждое существенное утверждение этого документа помечено одним из четырёх статусов:

- `SOURCE_NORMATIVE` — уже прямо утверждено существующим источником, цитируется или пересказывается без ослабления;
- `DECISION_CANDIDATE_FOR_REVIEW` — предлагается этим proposal как безопасный кандидат, не утверждено;
- `OPEN_BLOCKED_PENDING_DECISION` — источников недостаточно или требуется отдельное решение owner'а;
- `OUT_OF_SCOPE` — принадлежит другому артефакту или gate.

Precedent, предложение соседнего документа или техническая осуществимость не повышаются до нормативного решения. Кандидатный или открытый статус никогда не выдаётся за действующую норму.

---

## 2. Назначение, scope и non-goals

### 2.1. Назначение

`MATCHING_RISK_POLICY` — будущий governance-артефакт, определяющий: risk signals/factors и их допустимые источники; evidence eligibility для Risk; aggregation/calibration candidates; versioning; объяснимость; границу human-review escalation. `DECISION_CANDIDATE_FOR_REVIEW` для самой структуры документа.

Документ отражает по Architecture §37 вопросу №8 («Какие пороги Risk Score требуют обязательного human review?», owner `AI + LEGAL`, `SOURCE_NORMATIVE`) только human-approved qualitative governance/evidence `PARTIALLY_RESOLVED_BOUNDARY` из `XFR-D-M2` — не назначает trigger/threshold/mapping/route и не закрывает вопрос полностью.

### 2.2. Что Risk Policy не является (`OUT_OF_SCOPE` / прямые запреты)

Risk:

- не доказательство нарушения — `SOURCE_NORMATIVE`, Architecture §17: «не является доказательством нарушения»;
- не кредитный рейтинг, юридическое заключение, санкция, решение о плательщике/возврате/отказе — `SOURCE_NORMATIVE`, Architecture §17, §5 принцип 14: «Matching Engine не принимает решение о возврате, кредите, спорной неявке, санкции или взыскании»;
- не Match Score, не Reciprocal Score, не Confidence Score, не Hard Constraint result — `SOURCE_NORMATIVE`, Architecture §5 принципы 8–9;
- не Qualification routing и не human Decision Record — `OUT_OF_SCOPE`, принадлежит `MATCHING_QUALIFICATION_POLICY` (§18.1) и Legal/Decision Service (§40) соответственно;
- не `SAFE_PRESENTATION_POLICY`, не SLO, не Cost Policy — `OUT_OF_SCOPE`, §51/§54 Architecture;
- не даёт разрешения на implementation/runtime/API/DB changes.

### 2.3. Таксономия — 9 непересекающихся уровней

```
source fact
  → evidence status/quality/freshness
    → risk signal
      → risk factor/category
        → Risk output
          → (отдельно) Confidence
            → Hard Constraint result
              → Qualification routing
                → human Decision Record / legal conclusion
```

`SOURCE_NORMATIVE`: Architecture §5 принцип 8 требует отдельно хранить факт, предположение, вывод и риск («Факт, предположение, вывод и риск хранятся раздельно»); принцип 9 требует различать Match Score, Confidence Score и Risk Score как разные показатели. Это дословно устанавливает раздельность **четырёх** и **трёх** сущностей соответственно — не девятиуровневую схему выше.

`DECISION_CANDIDATE_FOR_REVIEW`: сама девятиуровневая детализация (разбиение на 9 конкретных уровней, включая, например, отдельность «risk signal» от «risk factor/category» или «Hard Constraint result» от «Qualification routing») и правило не сворачивать все девять уровней друг в друга — предложение этого документа, поддержанное принципами 8–9 выше, но не установленное источником буквально на этом уровне детализации.

---

## 3. Источники и классификация утверждений

Прочитаны полностью: `LeaseMind_MATCHING_ENGINE_ARCHITECTURE_v1.1.md` (включая §5, §6, §8.4, §9, §11–19, §27, §30–34, §36–37, §40, §48–54); `LeaseMind_MATCHING_DATA_CONTRACTS_v1.0.md` (проверено — не содержит ни одного Risk-специфичного name/enum, поэтому этот документ ничего не расширяет и не переопределяет там); `LeaseMind_MATCHING_FEATURE_SCHEMA_v0.1.md`; `LeaseMind_MATCHING_EVALUATION_PLAN_v0.1.md`; `CAMPAIGN_OUTCOMES.md`; `ANALYSIS_SNAPSHOT.md`; восьмой DEVELOPMENT review (только как DEVELOPMENT evidence).

Источник-приоритет: буквальный текст Architecture > буквальный текст CTA/CAMPAIGN_OUTCOMES/ANALYSIS_SNAPSHOT > Proposal-зависимости (Feature Schema, Evaluation Plan, оба неутверждённые) > DEVELOPMENT review (только техническая воспроизводимость, не архитектурное решение).

---

## 4. Ownership и boundary matrix

| Артефакт/роль | Владеет | Не владеет |
| --- | --- | --- |
| `MATCHING_RISK_POLICY` (этот документ) | Определения risk signals/factors, evidence eligibility, aggregation/calibration candidates, versioning, объяснимость, граница human-review escalation | Match/Reciprocal/Dimension arithmetic; финальный Qualification routing; какие поля показывать пользователю |
| `MATCHING_FEATURE_SCHEMA` | Какие feature/facts существуют, их evidence/freshness/applicability (Proposal, не утверждён) | Risk factor definitions, aggregation |
| `MATCHING_SCORING_POLICY` | Match/Reciprocal/Dimension arithmetic — не Risk | Risk aggregation |
| `MATCHING_QUALIFICATION_POLICY` | Итоговый routing в `QUALIFIED_HYPOTHESIS`/`NEEDS_VERIFICATION`/`HUMAN_REVIEW_REQUIRED`/`REJECTED_BY_MATCHING` (Architecture §18.1) | Risk factor definitions, evidence sufficiency |
| `MATCHING_EVALUATION_PLAN` | Dataset/labels/metrics/threshold-search evidence (Proposal, не утверждён) | Финальный Risk threshold |
| `SAFE_PRESENTATION_POLICY` | Что можно показать пользователю; может использовать Risk-related classification как input | Вычисление Risk, владение Risk calculation |
| LEGAL | Юридически значимые выводы; подтверждение критического риска; lawful sources/lawful basis для §37 №7 (`SOURCE_NORMATIVE`). В `XFR-D-054` отдельно участвует в candidate-derived governance owner pair `LEGAL + PRODUCT`, без превращения всей пары в source-normative authority | Sole technical reproducibility; unilateral substitution of evidence/governance roles |
| AI Manager | Orchestration задач Кампании | Подмена источника фактов, LEGAL, user decision (Architecture §5 принцип 2–4) |
| DEVELOPMENT | Reproducibility/technical feasibility — `DECISION_CANDIDATE_FOR_REVIEW`, если источник не назначает прямо | Смысл risk categories |
| Legal/Decision Service | Единственный writer мотивированных reviewer decisions (Architecture §40) | Risk calculation |
| Matching Engine | Единственный writer расчёта Match (включая Risk Score, §40) | Кампания, стратегия, плательщик (§5 принципы 3–4) |

**Отсутствие циклической зависимости.** Risk Policy может оцениваться против versioned candidate bundles Feature Schema/Scoring Policy до их утверждения — `DECISION_CANDIDATE_FOR_REVIEW`. Evaluation Plan производит evidence для последующего cross-functional approval конкретных Risk thresholds — не заменяет и не продвигает его автоматически (Architecture §34.4: «Автоматическое изменение продуктивных правил по результатам обучения — 0 случаев»).

---

## 5. 10-category boundary registry (Architecture §17 — ровно 10, дословно)

Каждая категория по названию — `SOURCE_NORMATIVE` (буквальный текст §17). Mapping/sufficiency/detection/freshness/owner — нормативны только там, где источник это прямо говорит; иначе `DECISION_CANDIDATE_FOR_REVIEW` или `OPEN_BLOCKED_PENDING_DECISION`.

| № | Категория | Status | Source fact/evidence | Automatic signal | Human/legal confirmation | Запрещённый вывод | Freshness | Соседний artifact/gate (справочно, не owner) |
|---|---|---|---|---|---|---|---|---|
| 1 | качество и конфликт данных | Категория `SOURCE_NORMATIVE`; mapping `DECISION_CANDIDATE_FOR_REVIEW`; sufficiency `OPEN_BLOCKED_PENDING_DECISION` | Версии значений, канонический `evidence_status` (Architecture §13) — candidate mapping | Флаг «источники противоречат» (`SOURCE_NORMATIVE`, §32) | Human review при критичности (`SOURCE_NORMATIVE`, §32); что есть «критичность» — `OPEN` | Автоматическое разрешение конфликта в пользу одной версии | Версии сохраняются, не перезаписываются (`SOURCE_NORMATIVE`, §32) | Feature Schema (evidence/freshness модель) |
| 2 | полномочия представителя | Категория `SOURCE_NORMATIVE`; single-writer `SOURCE_NORMATIVE`; mapping в Risk factor `DECISION_CANDIDATE_FOR_REVIEW` | Versioned projection Identity/Authority Registry (`SOURCE_NORMATIVE`, §40) | Read-only ссылка на внешний verified факт | Подтверждение — исключительно внешний Registry (`SOURCE_NORMATIVE`, §40) | Matching Engine самостоятельно не подтверждает/не исправляет authority | Invalidation events Identity/Authority Registry | Identity/Authority Registry |
| 3 | связь стороны с объектом | Категория `SOURCE_NORMATIVE`; lawful sources `OPEN_BLOCKED_PENDING_DECISION` (Architecture §37 №7, owner `LEGAL`); mapping `DECISION_CANDIDATE_FOR_REVIEW` | Допустимые lawful источники не определены — `OPEN` | Сигнал несоответствия — `DECISION_CANDIDATE_FOR_REVIEW` | Юридическая интерпретация — `LEGAL` (Architecture §37 №7, дословно: «Какие законные источники допускаются для проверки полномочий, связи с объектом и связанных лиц? \| LEGAL \| Qualification и Risk Policy») | Вывод о недобросовестности без review | Revision-bound (Feature Schema §8) — candidate mapping, не Architecture-норма для этой категории | `LEGAL` (§37 №7); Feature Schema (справочно) |
| 4 | дублирование сущностей | Категория `SOURCE_NORMATIVE`; `PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-056 v1.0` разрешает только decision-specific roles, advisory-only possible-duplicate, source-authority/history preservation, duplicate-dependent fail-closed и no-automatic-consequence boundary; exact taxonomy/mechanism/evidence/authority/runtime остаются `OPEN` | Similar/equal identifier, contact, address, device, fuzzy/AI output, event duplicate или `DUPLICATE_ENTITY_CONFIRMED` string сами по себе не являются доказательством | Возможный duplicate — только advisory candidate, не confirmed duplicate/identity fact/Risk/Hard Constraint/Qualification/legal/user-facing outcome | Future confirmation требует отдельно approved version/hash-bound mechanism, eligible lawful source evidence и separately appointed authority; exact reviewer/RBAC/quorum/appeal procedure остаётся `OPEN` | Нельзя автоматически merge/link/unlink/split/delete/discard/select survivor, reject/`INELIGIBLE`/route, ограничивать party/account/access/participation или переносить cross-entity facts/evidence/consent/requirements/outcomes/contact/protection/payment; unresolved evidence блокирует только duplicate-dependent action, не general access; Architecture §31 employee authority/appeal сохраняются | Не определена источником — `OPEN` | Governance owner `DEVELOPMENT + AI`; mandatory approvers `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical `AI + DEVELOPMENT` без unilateral approval (`XFR-D-056`) |
| 5 | операционная несовместимость | Категория `SOURCE_NORMATIVE` (буквальное название); scope `OPEN` — источник **не** определяет её как любой confirmed hard/soft mismatch и **не** говорит, что LEGAL review никогда не требуется | Не определено источником — `OPEN` | candidate, конкретные признаки не выбраны | Не определено источником — `OPEN` | Использование как proxy операционного риска/кредитоспособности — **precedent, не готовое правило**: Feature Schema реклассифицировала `business_stage_signal` в `EXCLUDED_FROM_V0_1` именно из-за такого proxy-риска до LEGAL+PRODUCT решения (Feature Schema §6.4, Architecture §14.3 условие 4) | Не определена — `OPEN` | Feature Schema (precedent, справочно); `LEGAL` там, где proxy-риск подтверждён |
| 6 | риск устаревания | Категория `SOURCE_NORMATIVE`; `STALE`-поведение `SOURCE_NORMATIVE` (§32); Risk factor/score formula и общий TTL `OPEN`, источник их не формулирует | `evidence_status`/`freshness_class` (Feature Schema §8, канонический §13) — candidate mapping | `STALE`-статус, дословно §32: «Профиль устарел → Match становится `STALE`; раскрытие по нему не разрешается» (`SOURCE_NORMATIVE`) | Не определено отдельно для этой категории | Использование stale-профиля как подтверждённого факта | TTL отсутствует в любом источнике — `OPEN`; связано с Architecture §37 №11 (`PRODUCT + LEGAL + AI`, шире) и отдельно с узким открытым решением №6 Feature Schema (`PRODUCT + AI`, через Evaluation Plan) — два разных пункта | Feature Schema (freshness classes) |
| 7 | риск повторной идентификации объекта до раскрытия | Категория `SOURCE_NORMATIVE`; detection technique `OPEN`/`DECISION_CANDIDATE_FOR_REVIEW`, не утверждённый механизм | Coarse geography — **один из возможных** input candidates (Feature Schema §7.4), не единственный или обязательный источник | Гипотетический «sparse combinations detector» — candidate технология, established mechanism ни один источник не подтверждает | Численный порог агрегации — `LEGAL`/`PRODUCT`, `OPEN` (Feature Schema №7, Evaluation Plan №9) | Публикация точного адреса/координат как результата Risk (`SOURCE_NORMATIVE` запрет, §9.4, §5 принцип 13) | Не определена — `OPEN` | Feature Schema (открытое решение №7), `SAFE_PRESENTATION_POLICY` |
| 8 | возможный прежний контакт | Категория `SOURCE_NORMATIVE`; mapping на `previous_contact_analysis_signal` `SOURCE_NORMATIVE` (дословная цитата §31) | `previous_contact_analysis_signal` (Feature Schema §7.2), только неитоговый | «Только сигнал и анализ» (`SOURCE_NORMATIVE`, §31 дословно) | «Итог подтверждает уполномоченный reviewer по доказательствам» (`SOURCE_NORMATIVE`, §31 дословно) | Автоматическое превращение AI-совпадения в доказанный previous contact (`SOURCE_NORMATIVE`, §34.4: 0 случаев) | Freshness `previous_contact_analysis_signal` относительно исходных данных — `OPEN_BLOCKED_PENDING_DECISION`; источник не формулирует | Previous Contact Gate (внешний, исключительно владеет финальным решением, §18.4), `LEGAL` |
| 9 | возможная связь лиц или обход — только как сигнал | Категория `SOURCE_NORMATIVE`, включая формулировку «только как сигнал» (дословно §17) | Не определено источником конкретно | Risk-сигнал, не решение (`SOURCE_NORMATIVE` формулировка) | «Решает сотрудник; взыскание — по LEGAL» (`SOURCE_NORMATIVE`, §31 дословно) | Автоматическое отнесение к «обходу» без review | Не определена численно — `OPEN` | `LEGAL`/reviewer (§31.1) |
| 10 | аномальное поведение, относящееся к конкретным проверяемым событиям | Категория `SOURCE_NORMATIVE`, но строго ограничена конкретными проверяемыми событиями (дословно: «относящееся к конкретным проверяемым событиям», не общий behavioral profile); freshness class и detection algorithm `OPEN` | Только события, относящиеся к конкретным проверяемым фактам — `SOURCE_NORMATIVE` ограничение scope | Флаг аномалии по конкретному событию — candidate | Причинная интерпретация аномалии требует review — candidate | Негативный вывод «только из свободного текста, поведенческого предположения или одного непроверенного источника» (`SOURCE_NORMATIVE`, §13 Architecture, дословно) | Класс freshness не утверждён ни одним источником — `OPEN` | Feature Schema (evidence, справочно); Risk Policy само после approval |

---

## 6. Hard Constraint / Risk / Confidence / Qualification boundary

`SOURCE_NORMATIVE` границы:

- подтверждённый Hard Constraint обрабатывается **до** scoring — Architecture §5 принцип 6, §14 этап 3 (Eligibility Filter предшествует Reciprocal Scoring);
- unknown не становится negative — §5 принцип 7, §12.4;
- conflicting/stale/inconclusive не становятся доказанным нарушением — §32 (три разные строки, см. §8 ниже);
- Confidence Score показывает надёжность оценки, а не привлекательность пары; Risk Score — проверяемые факторы, способные снизить реализуемость или потребовать проверки (§16–17, дословный смысл);
- высокий Match Score при низком Confidence Score требует проверки, не может быть представлен как готовый Квалифицированный вариант (§16, дословно);
- risk не компенсирует Hard Constraint (§5 принцип 6 — non-compensation) и не является доказательством нарушения (§17).

**Точная граница с Match Score:** «Match Score объединяет Reciprocal Fit и Deal Feasibility по утвержденной версии весов» (Architecture §15.6, дословно) — Risk Score в эту формулу **не входит**. Отдельный Priority Score **может** учитывать Match Score, Confidence Score и Risk Score вместе для ранжирования (§15.6); все исходные показатели сохраняются и показываются раздельно для аудита. Risk не скрывается внутри формулы Match Score.

Формула взаимодействия Risk/Confidence/Match этим документом не изобретается — `OPEN_BLOCKED_PENDING_DECISION`.

---

## 7. Evidence eligibility, provenance и freshness (концептуально, без DB/API)

Candidate contract (`DECISION_CANDIDATE_FOR_REVIEW`, по аналогии с Feature Schema §3, без изобретения нового поля сверх Architecture §11):

- immutable source/evidence reference;
- source owner/version;
- observed/verified time;
- evidence status (только канонический Architecture §13 enum, без добавлений);
- lawful-basis/purpose eligibility для применимых данных (Architecture §11, единственный writer — Lawful Basis/Consent Registry, §40);
- freshness/revocation/conflict handling;
- rule/policy version/hash;
- reason/explanation reference (см. §11 ниже — namespace open);
- явное различие между fact, inference и human decision (таксономия §2.3).

`SOURCE_NORMATIVE`: AI inference, similarity, anomaly или proxy не становятся confirmed fact автоматически (Architecture §5 принцип 8, §13: «Негативный вывод о стороне не создается только из свободного текста, поведенческого предположения или одного непроверенного источника»). Одного self-report недостаточно там, где Architecture требует verification/human decision (§27.2: «Одностороннее заявление и AI-вывод не становятся истинной меткой автоматически»).

**Current per-factor evidence-sufficiency overlay — `PARTIALLY_RESOLVED_BOUNDARY`, `XFR-D-049 v1.0`.** Governance owner `AI + LEGAL` is human-approved candidate-derived, not `SOURCE_NORMATIVE`; mandatory approvers are `Chief AI Architect + PRODUCT + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT` has no unilateral authority. Sufficiency must be evaluated separately for the exact factor/category/source/use/purpose/version against attributable, current and compatible provenance. The exact seven-value Architecture §13 `evidence_status` remains categorical, without numeric value, order or hierarchy; fact, inference, signal and human decision remain distinct. Missing/insufficient/unauthorized/stale/conflicting/incompatible material blocks only the affected factor/use and creates no clean/low/negative/violation/`INELIGIBLE`/route/legal inference. Self-report is not universally insufficient: it is insufficient only where an independently applicable approved rule requires verification or a human decision. No category is classified critical or review-required here. Exact sufficiency catalogs/levels/mappings, criticality, reviewer/RBAC, numeric/data/carrier/policy/production/runtime/implementation contents remain `OPEN`; `XFR-D-M1` is not imported into Risk by analogy.

---

## 8. Protected attributes, proxies и prohibited uses

`SOURCE_NORMATIVE`, Architecture §17 — **абсолютный запрет без исключений**: «Risk Score… не должен использовать защищенные признаки или прокси». Дополнительно §5 принцип 11, §30.3 пункт 4 (проверка дискриминационных признаков и прокси перед любым platform-level изменением).

**Роль LEGAL review — не «разрешение после проверки»:**

- LEGAL review определяет, является ли candidate признак protected/proxy;
- отдельно LEGAL review оценивает lawful basis уже подтверждённого **не-protected** источника/use;
- confirmed protected/proxy классификация означает exclusion/block, **не** approval after review;
- lawful basis сам по себе не отменяет architectural ban §17.

Явные прочие запреты (`SOURCE_NORMATIVE`, §17, §5, §6.2, §34.4): Risk Score не кредитный рейтинг; не юридическое доказательство; не санкция; не назначает плательщика; не решает refund/credit; не подтверждает previous contact/related persons/circumvention; не распространяет вывод между пользователями/сегментами без lawful basis и review.

Этот документ **не создаёт** legal fairness standard и не объявляет ни один конкретный признак допустимым.

**Current protected/proxy and lawful-basis overlay — `PARTIALLY_RESOLVED_BOUNDARY`, `XFR-D-054 v1.0`.** Decision-specific governance owner `LEGAL + PRODUCT` is human-approved candidate-derived and not `SOURCE_NORMATIVE` for the whole decision; mandatory approvers are `Chief AI Architect + AI + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT` has no unilateral authority. `LEGAL` separately retains source-normative lawful-source/lawful-basis authority under Architecture §37 question №7. For every exact feature/source/use/purpose/version, protected/proxy classification must occur first; only a positively confirmed non-protected/non-proxy candidate may proceed to a separate `LEGAL` lawful-source/lawful-basis determination. Confirmed protected/proxy means unconditional exclusion from the affected Risk use, and lawful basis cannot override that prohibition. Missing/unknown/unclassified/ambiguous/stale/conflicting/incompatible material blocks the affected use without adverse inference. AI/heuristics/proxy detectors/fairness diagnostics and an `ACTIVE` registry projection create neither classification nor lawful determination. Actual taxonomy/classification, proxy methods, lawful catalogs/determinations, purposes, evidence, reviewer/RBAC, data/contracts/carrier/Risk→Qualification/policy/production/runtime/implementation remain `OPEN`; Architecture §37 question №7 remains `OPEN`.

**Exact address/geography:** остаётся только в защищённом контуре, не появляется в открытых outputs/events/logs (`SOURCE_NORMATIVE`, §9.4, §5 принцип 13). Метод и численный порог re-identification — `OPEN_BLOCKED_PENDING_DECISION` (см. категорию 7, §5 выше).

**Pseudonymized ≠ anonymized** (`SOURCE_NORMATIVE`, §8.4, дословно): «Токенизированные ID, хешированные значения и псевдонимы остаются персональными данными, если LeaseMind… способен восстановить связь с субъектом. Они не считаются обезличенными…» Необратимое обезличивание перед segment-аналитикой требует шести шагов §8.4 (удаление прямых идентификаторов, обобщение/исключение редких комбинаций и точной географии/времени, исключение малых групп, проверка защищённых признаков, документирование метода/версии/даты, разрешение Data Governance) — цитируется без ослабления.

---

## 9. Aggregation/calibration candidates — сравнение, без выбора численной формулы

`DECISION_CANDIDATE_FOR_REVIEW` для точной численной формулы, ни один вариант численно не выбран:

| Вариант | Плюс | Минус / риск компенсации |
| --- | --- | --- |
| (a) Independent per-category flags | Максимальная прозрачность; ни один critical factor не скрыт | Не даёт единого сигнала для routing без отдельного правила Qualification Policy |
| (b) Max/worst-factor precedence | Гарантирует non-compensation (жёсткий фактор не размывается средним) | Может завышать риск при одном шумном факторе |
| (c) Explicit rule-based precedence | Явная, объяснимая логика приоритета | Требует ручного каталога правил, комбинаторно растёт |
| (d) Weighted aggregation | Гибкость калибровки | Требует весов (не вводятся этим документом); риск скрытой компенсации между факторами |
| (e) Multi-dimensional vector | Не сворачивает информацию | Требует, чтобы Qualification Policy умела читать вектор, не число |

**Qualitative boundary — `RESOLVED_QUALITATIVE_BOUNDARY`, human-approved governance decision `XFR-D-048 v1.0`.** Утверждена комбинация elements (a)/(e) (multi-component representation, concept-level, не runtime vector schema) и non-compensation-свойства (b), взятого как conditional overlay для категорий, отдельно классифицированных critical по будущим утверждённым правилам: (d) weighted aggregation целиком остаётся `OPEN` — numeric weights/thresholds/TTL/calibration targets deferred `XFR-D-050`/`XFR-D-M2`; (c) explicit rule-based precedence остаётся `DECISION_CANDIDATE_FOR_REVIEW`, не выбран и не отклонён. Ни одна численная формула/threshold/TTL не введена ни `XFR-D-048`, ни этим Proposal.

Handling missing/conflicting/stale evidence, monotonicity/non-compensation properties, explainability/reason codes, calibration evidence requirements и segment-specific policy prohibition (до отдельного evidence/LEGAL review) остаются candidate requirements, не выбранной численной формулой. `XFR-D-051 v1.0` даёт только `PARTIALLY_RESOLVED_BOUNDARY`: сохраняет distinct Architecture §32 behaviors, conditional-critical conflict review, whole-affected-Match `STALE`/no-disclosure protection, orthogonal `STALE` и отсутствие default/universal route; точные Risk states/mappings/blocked unit/cascade/route/precedence/retry/TTL/reviewer/RBAC/schema/carrier/runtime остаются `OPEN`.

**Разделение audit-требований:** общий audit bundle Architecture §33 (сохранение активных критериев, причин исключения, неизвестных/конфликтующих данных для **любого** расчёта Matching Engine) — `SOURCE_NORMATIVE`, применяется вне зависимости от Risk. То, что именно per-category Risk detail обязан оставаться отдельно видимым и non-compensating (не сворачиваться в единое непрозрачное число), теперь `RESOLVED_QUALITATIVE_BOUNDARY` (human-approved governance decision `XFR-D-048 v1.0`, по аналогии с §5 принципом 6) — не буквальная Architecture-норма и не approval этого Proposal.

Ни один weight/formula/threshold/TTL/calibration target не выбран. `XFR-D-047 v1.0` даёт Risk representation только `PARTIALLY_RESOLVED_BOUNDARY`: точные десять Architecture §17 category names сохраняются как semantic names, не runtime enum/ID catalog; `XFR-D-048` multi-component/conditional non-compensation сохраняется, components остаются separately visible, а будущий scalar может быть лишь supplement. Exact identifiers/enums/form/values/order/aggregation/formula/reason mapping/route/version compatibility/TTL/precision/serialization/schema/carrier/public/runtime contents остаются `OPEN`; unavailable representation не становится clean/low/zero/default/reason/route.

**Current Risk calibration evidence-governance overlay — `PARTIALLY_RESOLVED_BOUNDARY`, `XFR-D-050 v1.0`.** Governance owner `AI + DEVELOPMENT` is human-approved candidate-derived and aligned with the Evaluation Plan evidence-artifact owner, not Risk approval authority; mandatory approvers are `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical-procedure owner is `AI + DEVELOPMENT` without unilateral authority. A future Risk calibration claim requires a frozen immutable version/hash-bound Evaluation candidate and manifest as prerequisites only, baseline first, tuning/final isolation, compatible comparison, uncertainty reporting and separate segment/fairness evidence without compensation. Synthetic-only evidence creates no production calibration/readiness claim. Evaluation results never automatically update model, policy, route, runtime or gates. Exact dataset/labels/adjudication/population/units/segments/metrics/formulas/numerators/denominators/aggregation/baseline/target/threshold/tolerance/statistics/sample/run/result/verdict/policy/production/carrier/runtime/implementation remain `OPEN`; no dataset, manifest or evaluation result is approved.

---

## 10. Human-review и routing boundary (Architecture §37 №8)

`SOURCE_NORMATIVE` разделение:

- Evaluation Plan владеет процедурой поиска/сравнения candidate thresholds (`MATCHING_EVALUATION_PLAN` §9, не финальный owner);
- Risk Policy после cross-functional approval фиксирует Risk-specific rule/version — не этот документ;
- Qualification Policy владеет итоговым routing/precedence (`QUALIFIED_HYPOTHESIS`/`NEEDS_VERIFICATION`/`HUMAN_REVIEW_REQUIRED`/`REJECTED_BY_MATCHING`, §18.1);
- LEGAL определяет, какие факты требуют human/legal decision;
- human reviewer подтверждает критический риск по §31 («Confidence и Risk: автоматически как оценка… критический риск подтверждает сотрудник»), но не меняет policy произвольно — применяет утверждённую policy в пределах приказа/RBAC (§31.1, 7 обязательных пунктов).

**Signal ≠ routing decision.** Risk signal не является routing decision. Approved Risk + Qualification policies **могут** направить signal/risk на verification/human review согласно Architecture §17 (последняя строка: «Высокий риск переводит результат в `HUMAN_REVIEW_REQUIRED` либо `NEEDS_VERIFICATION` по утвержденной политике») — но это не означает, что любой signal обязательно идёт на review, и Risk Policy **не присваивает** самостоятельно ни один из четырёх Qualification routing statuses. Численный/качественный trigger, при котором это происходит, — `OPEN_BLOCKED_PENDING_DECISION`.

**Human-approved interface boundary — `PARTIALLY_RESOLVED_BOUNDARY`, `XFR-D-055 v1.0`.** Governance owner — `Chief AI Architect + AI`; mandatory approvers — `PRODUCT + LEGAL + DEVELOPMENT`; evidence/technical-procedure owner — `AI + DEVELOPMENT`, без unilateral approval. Risk output передаётся только как read-only conceptual input, связанный с identifiable policy/source/evidence/calculation versions. Только отдельно утверждённое совместимое Risk + Qualification rule может направить eligible Risk evidence в `HUMAN_REVIEW_REQUIRED` либо `NEEDS_VERIFICATION`; Risk output сам по себе никогда не создаёт `QUALIFIED_HYPOTHESIS` или `REJECTED_BY_MATCHING` и не создаёт legal outcome.

Missing, unknown, unmapped, incomplete, conflicting, stale или version-incompatible Risk input не считается clean/zero/low, negative fact или authorization; не угадывается и не coercion'ится. Он обрабатывается fail closed, но exact выбор route и granularity остаются `OPEN`. `XFR-D-048` multi-component/non-compensation, `XFR-D-033` Qualification precedence, `XFR-D-038` orthogonal `STALE` и `XFR-D-040` multi-cause semantics сохраняются без переоткрытия. Artifact owner Risk Policy остаётся `Chief AI Architect + LEGAL`, artifact owner Qualification Policy — `Chief AI Architect + PRODUCT`, а `XFR-D-M2` остаётся source-owned `AI + LEGAL`.

**Current missing/conflicting/stale overlay — `PARTIALLY_RESOLVED_BOUNDARY`, `XFR-D-051 v1.0`.** Governance owner is human-approved candidate-derived `AI + DEVELOPMENT`; mandatory approvers are `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical-procedure owner `AI + DEVELOPMENT` has no unilateral authority. Architecture §32 remains exact: missing/unknown leads to `NEEDS_VERIFICATION` and is not negative; conflicting versions remain separately preserved, Confidence is reduced without an invented number, and human review applies only when criticality is independently established; where the stale-profile rule applies, the whole affected Match becomes `STALE` and disclosure is prohibited. A narrower affected-factor/use rule may not weaken the stale whole-Match/no-disclosure effect. `STALE` remains an orthogonal Match state, not a fifth Qualification result and not an automatic Qualification `NEEDS_VERIFICATION`. Risk alone creates neither `QUALIFIED_HYPOTHESIS` nor `REJECTED_BY_MATCHING`; exact state/mapping/blocked-unit/cascade/route/precedence/retry/TTL/reviewer/RBAC/schema/carrier/runtime contents remain `OPEN`.

**Human-approved trigger-governance overlay — `PARTIALLY_RESOLVED_BOUNDARY`, merged `XFR-D-M2 v1.0`.** Risk signal, Risk trigger/rule и Qualification result остаются разными слоями. Будущий candidate должен быть одним closed explicit immutable version/hash-bound Risk + Qualification rule. Architecture разрешает только candidate directions `HUMAN_REVIEW_REQUIRED` или `NEEDS_VERIFICATION`, но этот record не выбирает ни одну из них и не задаёт mapping/cardinality. Missing/unmapped/stale/conflicting/incompatible rule/input/evidence fails closed только для затронутой risk-dependent progression; Risk не создаёт `QUALIFIED_HYPOTHESIS`, `REJECTED_BY_MATCHING`, `INELIGIBLE`, violation, sanction или legal outcome. Multi-component Risk, conditional non-compensation, Qualification precedence, multi-cause preservation, immutable history и запрет automatic policy/route/runtime/gate changes обязательны. Exact trigger/threshold/comparator/category, component/criticality/aggregation rules, mapping/route, cascade/granularity, data/statistics/evidence verdict, policies, production, schema/carrier/runtime/implementation остаются `OPEN`.

Human/legal outcome — отдельный Decision Record; единственный writer мотивированных reviewer decisions — Legal/Decision Service (Architecture §40).

**Human-approved reviewer-authority overlay — `PARTIALLY_RESOLVED_BOUNDARY`, `XFR-D-053 v1.0`.** Governance owner — `Chief AI Architect + LEGAL`; mandatory approvers — `PRODUCT + DEVELOPMENT + AI`; evidence/technical-procedure owner — `AI + DEVELOPMENT`, без unilateral approval. Governance owner, case reviewer, appointing authority, queue owner/operator и Legal/Decision Service writer остаются разными ролями. `HUMAN_REVIEW_REQUIRED` — только version-bound request к review, не подтверждение critical Risk, факт/санкция/access restriction, queue assignment, Decision Record или legal outcome. Human consequence требует separately approved applicable policy/rule, действующего письменного назначения и scope, least-privilege RBAC, eligible evidence и conflict-of-interest check до открытия case/любого evidence access с обязательной повторной check непосредственно перед решением; exact check method/evidence carrier остаётся `OPEN`.

Legal/Decision Service остаётся sole writer motivated outcome, а исходный Matching calculation/history — immutable. Queue transport, assignment, priority, SLA или completion не создают verdict/route/authority; reviewer не меняет ad hoc threshold, criticality, mapping, evidence sufficiency или routing. Missing/invalid authority, policy, evidence or required linkage fails closed для review-dependent consequence без confirmed critical/legal/reject/access outcome и без clean/zero/low inference; exact route/cascade остаётся `OPEN`. Payer correction после акцепта, авторизации, списания или применения кредита сохраняет отдельного независимого `PAYER_CORRECTION_APPROVER`; независимо, в любой separately approved four-eyes procedure reviewer и approver не могут быть одним лицом. Ни одно правило не создаёт universal quorum для всех Risk reviews. Architecture §21.7 — structural precedent only, без импорта dispute-specific schema/types/transitions; `XFR-D-041` остаётся distinct Qualification decision.

Без automatic promotion результатов evaluation в policy (§34.4: «Автоматическое изменение продуктивных правил по результатам обучения — 0 случаев»).

---

## 11. Versioning/replay/audit — концептуальный bundle

Concept-level состав (без проектирования storage), согласовано с Architecture §33 (`SOURCE_NORMATIVE` — точный audit-bundle для любого расчёта Matching Engine) и §49 (reproducibility spec):

- risk policy version/hash;
- feature/evidence schema version/hash;
- input/evidence snapshot hashes;
- exact reason/factor set;
- calculation method/version;
- code/build/model digest, где применимо;
- deterministic/recorded replay mode;
- human decision reference — отдельно от расчёта;
- freshness/revocation state на момент расчёта.

`DECISION_CANDIDATE_FOR_REVIEW`: Risk result history не переписывается задним числом; новое evidence/policy создаёт новый calculation/result. Architecture §33 (immutability decision events — «Исходные события не изменяются после решения» относится к юридическим/decision events, не к Risk calculation вообще) и `CAMPAIGN_OUTCOMES.md` §7 (append-only принцип другой сущности, `business_outcome`) — только supporting precedents, не прямая Risk-норма; источник не формулирует append-only/history правило именно для Risk result.

`SOURCE_NORMATIVE`, в точной мере, которую устанавливает Architecture §49: exact replay обязателен для deterministic path; несовпадение — severity-1 defect, блокирует соответствующую версию правил (§49, дословно); недетерминированный компонент не проходит Matching Qualification Gate самостоятельно, используется только как advisory signal до human-confirmed deterministic rule (§49, дословно).

**Bounded replay — current human-approved overlay (`XFR-D-M4 v1.0`, `PARTIALLY_RESOLVED_BOUNDARY`).** Canonical merged identity `EP-10 + MRP-11 + MQP-15 + MSP-14 → XFR-D-M4` сохраняет все four source keys as `PRIMARY_MERGED_MEMBER`. Governance owner `DEVELOPMENT + AI` is human-approved candidate-derived, not `SOURCE_NORMATIVE`; mandatory approvers `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical owner `DEVELOPMENT + AI` has no unilateral authority; Risk artifact owner remains separately `Chief AI Architect + LEGAL`, while Evaluation, Qualification and Scoring artifact owners also remain separate. Deterministic replay remains exact-only with no tolerance; mismatch remains severity-1 and blocks the affected rule version. Recorded replay and bounded probabilistic replay are distinct; probabilistic output stays advisory and cannot alone pass Qualification. A future bounded candidate must be one closed immutable version/hash-bound specification and evidence package with preregistration, frozen allocation, tuning/final isolation, compatible comparison and separate per-component/invariant reporting. Missing/stale/conflicting/incompatible replay material fails closed for affected use without invented tolerance/pass/negative fact/route; aggregate success cannot mask component/invariant failure; a replay creates a new audit event and does not mutate historical Match Result; no result automatically changes policy/model/runtime/gate. Every exact tolerance value/range/unit/error metric/direction, absolute/relative/per-component/aggregate treatment, reason-code invariant catalog, probabilistic eligibility, recorded-artifact schema/retention/provenance, dataset/splits/sample/metrics/targets/statistics, representation/precision/rounding, policy/manifest approval, production applicability, API/DB/events/carrier/runtime/monitoring/rollback/implementation remains `OPEN_BLOCKED_PENDING_DECISION`. `XFR-D-020`/`023`/`026`/`027`, `XFR-D-057`–`071` and all Risk/Qualification/Scoring/Evaluation boundaries remain independent.

**Reason references — current human-approved overlay (`XFR-D-052 v1.0`, `PARTIALLY_RESOLVED_BOUNDARY`):** governance owner `Chief AI Architect + AI`; mandatory approvers `PRODUCT + LEGAL + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT` без unilateral approval. Будущая Risk reason reference остаётся internal opaque reference, distinct от Risk category/severity, Architecture §25.1/§25.2/§25.3, Hard Constraint `XFR-D-010`, Qualification `XFR-D-039`, reviewer/legal outcome и user-facing `XFR-D-077`; equality/prefix/similar wording не создают alias/mapping. Future use требует separately approved meaning/applicability и binding к Risk Policy version/hash, source/evidence и original calculation/result provenance; historical bundle immutable, changed meaning требует new approved version + new calculation. Missing/unknown/unmapped/ambiguous/conflicting/stale/version/hash/scope-incompatible reference fails closed без clean/low/negative/legal/route/display inference и без history mutation; exact handling остаётся `OPEN` под `XFR-D-051`/`XFR-D-055`. `XFR-D-048` multi-component/non-compensation сохраняется. Exact namespace/name/codes/values/order/severity/mappings/compatibility/change/fallback/TTL/hash fields/carrier/data/evidence/RBAC/policy/manifest/runtime/implementation остаются `OPEN`; публичный каталог кодов и runtime carrier не вводятся.

**Risk output representation — current human-approved overlay (`XFR-D-047 v1.0`, `PARTIALLY_RESOLVED_BOUNDARY`).** Governance owner `Chief AI Architect + AI` is human-approved candidate-derived and not `SOURCE_NORMATIVE`; mandatory approvers are `PRODUCT + LEGAL + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT` has no unilateral authority. The exact ten Architecture §17 category names remain semantic category names, not a runtime enum, identifier catalog or public vocabulary. `XFR-D-048` multi-component and conditional non-compensation remain mandatory: future components stay separately visible and attributable, while any scalar may only supplement and never replace/mask them. A future representation must be immutable version/hash/provenance-bound and compatible at each governed use. Risk representation remains distinct from Match, Confidence, Hard Constraint, Qualification, legal/reviewer and presentation layers. Unavailable representation creates no clean/low/zero/default/reason/route inference. Канонического `LOW/MEDIUM/HIGH` или иного severity enum по-прежнему не существует; слова «высокий риск» и «критический риск» остаются качественным source text, не enum/threshold. Exact identifiers/enums/form/values/order/aggregation/formula/reason mapping/route/version compatibility/TTL/precision/serialization/schema/carrier/public/runtime contents remain `OPEN`.

---

## 12. Gates и acceptance boundary

Различены (`SOURCE_NORMATIVE` структура, Architecture §36):

1. Risk Policy proposal reviewed — cross-functional review этого документа;
2. Risk calculation reproducible — техническая воспроизводимость (DEVELOPMENT evidence);
3. Candidate threshold evaluation complete — Evaluation Plan procedure evidence;
4. Exact Risk Policy approved — отдельное cross-functional решение, не этот Proposal;
5. Qualification Policy approved — отдельный артефакт;
6. `IMPLEMENTATION_READINESS_GATE` / `SYNTHETIC_ACCEPTANCE_GATE` / `PRODUCTION_LAUNCH_GATE` — Architecture §36.

Успех ранней стадии не открывает следующую. Ни одна стадия выше не достигнута этим документом.

**Synthetic ≠ production evidence.** `SOURCE_NORMATIVE`: конкретные PRODUCT exclusions synthetic outcomes/records из real statistics/training/calibration (`CAMPAIGN_OUTCOMES.md` `CO-C-019`, `ANALYSIS_SNAPSHOT.md` `AS-C-019/025`); Matching synthetic-only boundaries и gate statuses (Architecture §36, §50 `NON_PRODUCTION_SAFETY_PROFILE`). Фраза «Synthetic Risk evaluation не является production calibration/readiness evidence» — `DECISION_CANDIDATE_FOR_REVIEW`, поддержанная указанными precedents, не выдана за дословную Risk-специфичную норму.

Controlled-set manifest этим документом не синхронизируется.

### Итоговый статус gates

- `IMPLEMENTATION_READINESS_GATE`: **`BLOCKED`**
- `SYNTHETIC_ACCEPTANCE_GATE`: **`BLOCKED`**
- `PRODUCTION_LAUNCH_GATE`: **`BLOCKED`**

Architecture §37 вопрос №8 остаётся **`OPEN`**.

`XFR-D-052` закрывает только partial qualitative governance boundary строки §13 №7; exact namespace/name/codes/values/order/severity/mappings/compatibility/change/fallback/TTL/hash fields/carrier/data/evidence/RBAC/policy/manifest/runtime/implementation остаются **`OPEN`**.

`XFR-D-056` закрывает только partial qualitative governance/authority boundary строки §13 №13: possible-duplicate остаётся advisory, source authority/history сохраняются, unresolved evidence блокирует только duplicate-dependent action, а automatic entity/access/Eligibility/routing/legal/cross-entity consequences запрещены. Exact entity taxonomy/keys/mechanism/algorithm/threshold/evidence sufficiency/confirming authority/reviewer procedure/schema/carrier/data/policy/runtime/implementation остаются **`OPEN`**. `XFR-D-048` non-compensation применим к duplicate Risk component только после independently approved critical classification и не делает possible/confirmed duplicate critical.

`XFR-D-053` закрывает только partial qualitative reviewer-authority/queue non-authority/Decision-linkage boundary строки §13 №8. Exact case reviewer/appointing authority/names/appointments/RBAC/quorum/queue owner/topology/SLA/outcomes/evidence/Decision Record applicability/schema/appeal/carrier/data/runtime/implementation остаются **`OPEN`**; `HUMAN_REVIEW_REQUIRED` остаётся request-only, `XFR-D-041` distinct, а ни один gate не снят.

`XFR-D-M4` закрывает только qualitative replay-governance/evidence boundary строки §13 №11. Exact deterministic replay остаётся без tolerance и с severity-1 mismatch; every bounded tolerance/invariant/data/statistical/policy/production/carrier/runtime/implementation content remains **`OPEN`**. No Proposal or gate is approved; all three gates remain **`BLOCKED`**.

`XFR-D-M3` закрывает только qualitative governance/evidence-prerequisite boundary строки §13 №10. Exact re-identification method/threshold and all method/cohort/quasi-ID/adversary/linkage/numerator/denominator/counting/statistical/data/evidence/policy/production/schema/carrier/runtime/implementation contents remain **`OPEN`**; all four source keys remain `PRIMARY_MERGED_MEMBER`, and all three gates remain **`BLOCKED`**.

Merged `XFR-D-M2` закрывает только `PARTIALLY_RESOLVED_BOUNDARY` qualitative governance/evidence safeguards строки §13 №4. Exact trigger, threshold, comparator, mapping, route, data/statistics, Risk/Qualification policy approval, production, carrier, runtime and implementation remain **`OPEN`**; all three gates remain **`BLOCKED`**.

`XFR-D-047`/`XFR-D-049`/`XFR-D-050`/`XFR-D-051`/`XFR-D-054 v1.0` закрывают только соответствующие `PARTIALLY_RESOLVED_BOUNDARY` строк §13 №1/№3/№5/№6/№9: Risk representation discipline, contextual evidence-sufficiency discipline, calibration evidence prerequisites, exact preservation of Architecture §32 behaviors и protected/proxy→lawful two-stage authority/order. Every exact identifier/enum/mapping/criticality/number/dataset/statistical method/result/lawful determination/reviewer/RBAC/schema/carrier/policy/production/runtime/implementation content remains **`OPEN`**. Frozen candidate/manifest prerequisites are not approvals; synthetic evidence is not production evidence; no result changes model/policy/route/runtime/gate automatically; `STALE` remains orthogonal with whole-affected-Match no-disclosure; confirmed protected/proxy exclusion is unconditional, while `LEGAL` separately retains source-normative lawful authority and Architecture §37 question №7 remains `OPEN`. No Proposal, Policy, Data Contract, dataset, evaluation run/result, production-data use, runtime or implementation is approved, and all three gates remain **`BLOCKED`**.

---

## 13. Open decisions

| № | Вопрос | Owner | Блокирует |
| --- | --- | --- | --- |
| 1 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-047 v1.0`.** Утверждены decision-specific roles; точные десять Architecture §17 category names остаются semantic names, не runtime enum/ID catalog; future representation требует immutable version/hash/provenance binding, separately visible components и scalar-supplement-only discipline; `XFR-D-048` multi-component/conditional non-compensation и separation от Match/Confidence/Hard Constraint/Qualification/legal/reviewer сохраняются; unavailable representation не создаёт clean/low/zero/default/reason/route. Exact identifiers/enums/form/values/order/aggregation/formula/reason mapping/route/version compatibility/TTL/precision/serialization/schema/carrier/public/runtime contents remain `OPEN` | Governance owner `Chief AI Architect + AI` — human-approved candidate-derived, not `SOURCE_NORMATIVE`; mandatory approvers `PRODUCT + LEGAL + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral authority. Risk artifact owner remains separately `Chief AI Architect + LEGAL` | Qualitative representation discipline больше не blocking; exact representation/public-runtime contents remain Explainability/`SAFE_PRESENTATION_POLICY` compatibility boundary |
| 2 | **`RESOLVED_QUALITATIVE_BOUNDARY` — `XFR-D-048 v1.0`.** Qualitative aggregation/non-compensation boundary утверждена: multi-component representation ((a)/(e)) + conditional non-compensation invariant ((b)-overlay) для отдельно классифицированных critical категорий; (d) weighted aggregation, численная формула, runtime representation и Risk→Qualification interface остаются `OPEN` | `Chief AI Architect + LEGAL` — approved decision-specific owner assignment `XFR-D-048 v1.0` (mandatory approvers `PRODUCT + DEVELOPMENT`, consulted `AI`); источник до этого record указывал только `Chief AI Architect + AI` как candidate assignment | Risk Score arithmetic — numeric часть остаётся blocking; qualitative aggregation/non-compensation semantics больше не blocking |
| 3 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-049 v1.0`.** Утверждены decision-specific roles и separate factor/category/source/use/purpose/version sufficiency discipline с attributable/current/compatible provenance; exact seven-value Architecture §13 evidence enum сохраняется categorical без numeric order; fact/inference/signal/human decision остаются distinct; unavailable material blocks only affected factor/use without adverse inference; self-report не universally insufficient, а insufficient только где separately applicable approved rule требует verification/human decision. No category becomes critical/review-required. Exact catalogs/levels/mappings/criticality/reviewer/RBAC/numeric/dataset/carrier/policy/production/runtime/implementation remain `OPEN` | Governance owner `AI + LEGAL` — human-approved candidate-derived, not `SOURCE_NORMATIVE`; mandatory approvers `Chief AI Architect + PRODUCT + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral authority. Risk artifact owner remains separately `Chief AI Architect + LEGAL` | Qualitative evidence-sufficiency discipline больше не blocking; exact sufficiency/criticality/human-confirmation contents remain blocking |
| 4 | **`PARTIALLY_RESOLVED_BOUNDARY` — merged `XFR-D-M2 v1.0` (`MRP-04`, `PRIMARY_MERGED_MEMBER`).** Approved only: authority split, Risk signal/trigger/Qualification-result separation, two source-authorized candidate directions without selection, immutable version/hash candidate discipline, affected-progression fail-closed, multi-component/non-compensation/precedence/multi-cause/history/no-automatic-action safeguards and qualitative evidence prerequisites. Every exact trigger/threshold/comparator/category/criticality/aggregation/mapping/route/cascade/data/statistical/policy/production/carrier/runtime/implementation content remains `OPEN` | Substantive owner `AI + LEGAL` (`SOURCE_NORMATIVE`, Architecture §37 №8); Risk artifact owner `Chief AI Architect + LEGAL`; Qualification artifact owner `Chief AI Architect + PRODUCT` (`XFR-D-030`); mandatory approvers `Chief AI Architect + PRODUCT + DEVELOPMENT`; evidence owner `AI + DEVELOPMENT` without unilateral authority; future exact change requires all five functions | Qualitative governance boundary no longer blocking; exact Risk→Qualification operationalization and all gates remain blocking |
| 5 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-050 v1.0`.** Утверждены decision-specific roles и только immutable version/hash-bound frozen Evaluation candidate+manifest prerequisites, baseline-first, tuning/final isolation, compatible comparison, uncertainty and separate segment/fairness reporting without compensation, synthetic-not-production and no-automatic-action safeguards. Exact dataset/labels/adjudication/population/units/segments/metrics/formulas/numerators/denominators/aggregation/baseline/target/threshold/tolerance/statistics/sample/run/result/verdict/policy/production/carrier/runtime/implementation remain `OPEN`; candidate/manifest are not dataset/evidence/result approval | Governance owner `AI + DEVELOPMENT` — human-approved candidate-derived and aligned with Evaluation evidence-artifact ownership, not Risk approval authority; mandatory approvers `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral authority. Risk artifact owner remains separately `Chief AI Architect + LEGAL` | Qualitative calibration-evidence discipline больше не blocking; all exact calibration and model-release contents remain blocking |
| 6 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-051 v1.0`.** Утверждены decision-specific roles and exact preservation/non-collapse of Architecture §32: missing/unknown → `NEEDS_VERIFICATION`, not negative; conflicts preserve versions/reduce Confidence without invented number and require review only when independently critical; applicable stale profile makes the whole affected Match `STALE` and prohibits disclosure. Affected-use language cannot weaken stale whole-Match/no-disclosure; `STALE` is orthogonal, not a fifth Qualification result or automatic Qualification route; Risk alone creates no `QUALIFIED_HYPOTHESIS`/`REJECTED_BY_MATCHING`. Exact Risk states/mappings/blocked unit/cascade/route/precedence/retry/TTL/reviewer/RBAC/schema/carrier/runtime/implementation remain `OPEN` | Governance owner `AI + DEVELOPMENT` — human-approved candidate-derived, not `SOURCE_NORMATIVE`; mandatory approvers `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral authority. Risk artifact owner remains separately `Chief AI Architect + LEGAL` | Qualitative source-behavior preservation больше не blocking; exact Risk operationalization/reproducibility remains blocking |
| 7 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-052 v1.0`.** Утверждены decision-specific roles, opaque internal reference/namespace separation, separately approved version/hash/provenance-bound use, immutable history/new-version-new-calculation discipline, fail-closed reference eligibility без clean/low/negative/legal/route/display inference, сохранение `XFR-D-048` multi-component/non-compensation и prerequisite-not-authorization boundary. Exact namespace/name/codes/values/order/severity/mappings/compatibility/change/fallback/TTL/hash fields/carrier/data/evidence/RBAC/policy/manifest/runtime/implementation остаются `OPEN` | Governance owner `Chief AI Architect + AI`; mandatory approvers `PRODUCT + LEGAL + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral approval. Risk artifact owner `Chief AI Architect + LEGAL`, Qualification owner `Chief AI Architect + PRODUCT`, `XFR-D-055` roles и `XFR-D-M2` owner `AI + LEGAL` не изменены | Qualitative governance boundary больше не blocking; exact contents остаются Qualification explainability/implementation boundary |
| 8 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-053 v1.0`.** Утверждены decision-specific roles, `HUMAN_REVIEW_REQUIRED` request-only semantics, разделение governance/case-reviewer/appointing/queue/Legal-writer authority, separately approved version/hash-bound policy/rule + valid written appointment/scope + least-privilege RBAC + eligible-evidence prerequisites, conflict check до открытия case/любого evidence access и повторно непосредственно перед решением, Legal/Decision Service sole-outcome-writer и immutable Matching calculation, queue/SLA non-authority, no-ad-hoc-policy, fail-closed без clean/low/invented route/access outcome, separate payer-specific second-level и applicable four-eyes reviewer≠approver controls без universal quorum, §21.7 structural-only и `XFR-D-041` distinct boundaries. Exact appointments/RBAC/queue/SLA/outcomes/evidence/Decision schema/appeal/carrier/data/runtime/implementation остаются `OPEN` | Governance owner `Chief AI Architect + LEGAL`; mandatory approvers `PRODUCT + DEVELOPMENT + AI`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral approval. Case reviewer, appointing authority, queue owner/operator и Legal/Decision Service writer не conflated | Qualitative governance boundary больше не blocking; exact per-class authority/linkage/operationalization остаётся §31.1/implementation boundary |
| 9 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-054 v1.0`.** Утверждены decision-specific roles, mandatory two-stage order and authority separation: protected/proxy classification first; only positively confirmed non-protected/non-proxy candidate proceeds to separate `LEGAL` lawful-source/lawful-basis determination for the exact feature/source/use/purpose/version. Confirmed protected/proxy exclusion is unconditional and lawful basis cannot override. Missing/unknown/unclassified/ambiguous/stale/conflicting/incompatible material blocks affected use without adverse inference. AI/heuristics/proxy detectors/fairness diagnostics/`ACTIVE` projection create no classification or lawful determination. Exact taxonomy/catalog/classifications/proxy methods/lawful catalog/determinations/purposes/evidence/reviewer/RBAC/data/contracts/carrier/Risk→Qualification/policy/production/runtime/implementation remain `OPEN` | Governance owner `LEGAL + PRODUCT` — human-approved candidate-derived and not `SOURCE_NORMATIVE` for the whole decision; mandatory approvers `Chief AI Architect + AI + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral authority. `LEGAL` separately remains source-normative lawful-source/lawful-basis owner; Risk artifact owner remains `Chief AI Architect + LEGAL` | Qualitative authority/order/exclusion/fail-closed boundary больше не blocking; Architecture §37 №7 actual lawful-source review, classifications/determinations and all operational contents remain blocking |
| 10 | **`PARTIALLY_RESOLVED_BOUNDARY` — merged `XFR-D-M3 v1.0` (`MRP-10`, `PRIMARY_MERGED_MEMBER`).** Только role split, four-context separation, closed immutable version/hash-bound candidate discipline, affected-use fail-closed, non-compensation, source-normative deny preservation, historical binding, no-automatic-action и qualitative evidence prerequisites утверждены; exact method/cohort/quasi-ID/adversary/linkage/numerator/denominator/counting/threshold/comparator/tolerance/statistics/data/evidence/policy/production/schema/runtime/implementation остаются `OPEN` | `PRODUCT + LEGAL` — human-approved candidate-derived governance owner, не `SOURCE_NORMATIVE` для whole merge; Risk artifact owner `Chief AI Architect + LEGAL`; mandatory approvers `Chief AI Architect + AI + DEVELOPMENT`; evidence/technical-procedure `AI + DEVELOPMENT` без unilateral authority | Exact re-identification method/threshold, Risk signal semantics, evidence and production applicability |
| 11 | **`PARTIALLY_RESOLVED_BOUNDARY` — merged `XFR-D-M4 v1.0` (`MRP-11`, `PRIMARY_MERGED_MEMBER`).** Approved only: deterministic exact/no-tolerance/severity-1 preservation; recorded-versus-bounded probabilistic replay separation; advisory-only probabilistic use; closed immutable version/hash-bound candidate/evidence discipline; preregistration, frozen allocation, tuning/final isolation, compatible comparison, separate reporting; affected-use fail-closed; non-compensation/no masking; historical Match Result immutability; no automatic action | `DEVELOPMENT + AI` — human-approved candidate-derived governance owner, not `SOURCE_NORMATIVE`; mandatory approvers `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical owner `DEVELOPMENT + AI` without unilateral authority; Evaluation/Risk/Qualification/Scoring artifact owners remain separate | Every exact tolerance value/range/unit/error metric/direction, absolute/relative/per-component/aggregate treatment, reason-code invariant catalog, probabilistic eligibility, recorded-artifact schema/retention/provenance, dataset/splits/sample/metrics/targets/statistics, representation/precision/rounding, policy/manifest approval, production applicability, API/DB/events/carrier/runtime/monitoring/rollback/implementation remains `OPEN`; exact contents still block replay/determinism acceptance |
| 12 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-055 v1.0`.** Governance/authority, provenance/version-bound read-only hand-off, permitted qualitative directions только в `HUMAN_REVIEW_REQUIRED`/`NEEDS_VERIFICATION` через отдельно approved compatible Risk + Qualification rule, Risk-alone prohibition для `QUALIFIED_HYPOTHESIS`/`REJECTED_BY_MATCHING`, fail-closed/non-compensation и prerequisite-not-authorization semantics утверждены. Exact trigger/mapping, numeric content, schema/carrier, compatibility/TTL/data/evidence/RBAC/policies/runtime/implementation остаются `OPEN` | Governance owner `Chief AI Architect + AI`; mandatory approvers `PRODUCT + LEGAL + DEVELOPMENT`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral approval. Risk artifact owner `Chief AI Architect + LEGAL`, Qualification artifact owner `Chief AI Architect + PRODUCT` и `XFR-D-M2` owner `AI + LEGAL` не изменены | Qualitative interface boundary больше не blocking; exact operationalization остаётся Implementation boundary |
| 13 | **`PARTIALLY_RESOLVED_BOUNDARY` — `XFR-D-056 v1.0`.** Утверждены decision-specific owner/approver/evidence roles, advisory-only possible-duplicate, separately approved version/hash mechanism + eligible source evidence + appointed authority prerequisites, source-authority/history preservation, duplicate-dependent-only fail-closed boundary и запреты automatic merge/link/unlink/split/delete/discard/survivor/reject/`INELIGIBLE`/route/access restriction/legal outcome/cross-entity transfer. Architecture §31 employee authority/appeal сохраняются; delivery/event deduplication отдельно; `XFR-D-048` действует только после independent approved critical classification | Governance owner `DEVELOPMENT + AI`; mandatory approvers `Chief AI Architect + PRODUCT + LEGAL`; evidence/technical-procedure owner `AI + DEVELOPMENT`, без unilateral approval. Risk/Qualification artifact owners, Identity/Authority Registry and Legal/Decision writers, `XFR-D-010`, `XFR-D-055` и `XFR-D-M2` не изменены | Qualitative governance boundary больше не blocking; exact entity taxonomy/keys/algorithm/features/threshold/confidence/evidence sufficiency/confirming authority/reviewer RBAC/quorum/appeal/correction/merge-split/schema/carrier/data/policy/runtime/implementation остаются `OPEN` |
| 14 | Численный TTL для «устаревание» (категория 6) — разведены Architecture §37 №11 (`PRODUCT + LEGAL + AI`, шире) и узкое открытое решение №6 Feature Schema (`PRODUCT + AI`, через Evaluation Plan) | `PRODUCT + LEGAL + AI` (§37 №11) / `PRODUCT + AI` (Feature Schema №6) — не выбирается здесь | Freshness-based risk signal |

Список не закрывается произвольно. В каждой из 14 строк owner либо имеет прямую source-ссылку (№4 — Architecture §37 №8; №14 — Architecture §37 №11 и отдельно Feature Schema открытое решение №6, разведены, не объединены в один owner), либо остаётся human-approved candidate-derived и explicitly not source-normative assignment (№1 `XFR-D-047`, №3 `XFR-D-049`, №5 `XFR-D-050`, №6 `XFR-D-051`, №9 `XFR-D-054`, №10 `XFR-D-M3`). №2 — approved decision-specific owner assignment `XFR-D-048 v1.0`; №7 — approved decision-specific assignment `XFR-D-052 v1.0`; №8 — approved decision-specific assignment `XFR-D-053 v1.0`; №11 — human-approved candidate-derived, explicitly not source-normative assignment `XFR-D-M4 v1.0`; №12 — approved decision-specific interface assignment `XFR-D-055 v1.0`; №13 — approved decision-specific assignment `XFR-D-056 v1.0`. `LEGAL` separately remains source-normative lawful-source/lawful-basis authority for row №9. Эти assignments сознательно не изменяют artifact owners Evaluation/Risk/Qualification/Scoring, source writers или source-owned `XFR-D-M2` threshold authority.

> **Temporal hygiene for row №10:** historical wording that calls the re-identification method/threshold question wholly `OPEN` is superseded by the current `XFR-D-M3` overlay: only the qualitative boundary is `PARTIALLY_RESOLVED_BOUNDARY`; every exact method, threshold, data, evidence, policy, production and runtime content remains `OPEN`.

---

## 14. Acceptance criteria (`MRP-C-001`–`MRP-C-018`)

#### `MRP-C-001` — `LOW/MEDIUM/HIGH` не изобретён; качественные слова ≠ enum
**Given:** документ описывает Risk output и цитирует качественный язык Architecture («высокий риск», «критический риск», §17/§31). **When:** запрашивается category enum. **Then:** `XFR-D-047 v1.0` разрешает только `PARTIALLY_RESOLVED_BOUNDARY` representation discipline: точные десять Architecture category names остаются semantic names, не runtime enum/ID catalog; «высокий»/«критический» остаются качественными формулировками, не enum-значениями/thresholds; every exact identifier/enum/value/representation/carrier/runtime content remains `OPEN`.

#### `MRP-C-002` — signal не становится fact/sanction/rejection, но approved policies могут направить его на review
**Given:** Risk category даёт сигнал (включая anomaly/similarity/proxy-подобный вход). **When:** формируется вывод. **Then:** сигнал сам по себе не становится доказанным фактом, санкцией или отказом; approved Risk + Qualification policies могут направить сигнал на verification/human review (§17, последняя строка), но это не превращает сигнал в подтверждённый факт (§17: «не является доказательством нарушения»). Для category 4 `XFR-D-056 v1.0` дополнительно запрещает possible-duplicate или unresolved evidence автоматически превращать в confirmed duplicate, merge/link/unlink/delete/discard, `INELIGIBLE`/route/legal outcome, party/account/access/participation restriction или cross-entity fact/evidence/consent/outcome transfer; fail-closed блокирует только duplicate-dependent action, general access не ограничивается, Architecture §31 employee authority и appeal сохраняются.

#### `MRP-C-003` — previous-contact signal не признан фактом
**Given:** Matching Engine производит `previous_contact_analysis_signal`. **When:** используется downstream. **Then:** остаётся «только сигналом и анализом»; итог подтверждает только уполномоченный reviewer (§31, дословно).

#### `MRP-C-004` — distinct behavior для missing, conflicting и stale без negative-label collapse
**Given (часть 1 — missing/unknown):** evidence отсутствует. **When:** вычисляется Risk. **Then:** не negative; может вести к `NEEDS_VERIFICATION` в точном контексте источника (§5 принцип 7, §12.4, §32).
**Given (часть 2 — conflicting):** источники противоречат. **When:** вычисляется Risk. **Then:** версии сохраняются, Confidence снижается, human review при критичности (§32, дословно).
**Given (часть 3 — stale):** профиль устарел. **When:** вычисляется Risk. **Then:** Match становится `STALE`, раскрытие запрещено (§32, дословно).
**Then (все три части):** `XFR-D-051 v1.0` сохраняет эти три source behaviors как `PARTIALLY_RESOLVED_BOUNDARY`: ни один случай не становится proved violation/negative label; review для conflict не automatic, а только при independently established criticality; affected-use handling не ослабляет whole-affected-Match `STALE`/no-disclosure; `STALE` orthogonal, не пятый Qualification result и не automatic Qualification `NEEDS_VERIFICATION`; Risk Policy не заменяет behaviors одним universal route. Exact operational contents remain `OPEN`.

#### `MRP-C-005` — Risk и Confidence разделены
**Given:** Match имеет одновременно Risk Score и Confidence Score. **When:** формируется explanation. **Then:** оба сохраняются и показываются раздельно для аудита (§15.6, §5 принцип 9); ни один не подменяет другой.

#### `MRP-C-006` — Risk не скрыто подмешан в Match arithmetic
**Given:** расчёт Match Score. **When:** Risk Score вычислен. **Then:** «Match Score объединяет Reciprocal Fit и Deal Feasibility по утвержденной версии весов» (§15.6, дословно) — Risk Score в эту формулу не входит; отдельный Priority Score может учитывать Match/Confidence/Risk вместе для ранжирования (§15.6), но все показатели остаются раздельно видимыми.

#### `MRP-C-007` — Risk Policy не присваивает Qualification routing
**Given:** Risk Policy формирует Risk output. **When:** формируется финальный routing status. **Then:** `XFR-D-055 v1.0` разрешает только provenance/version-bound read-only hand-off: конкретное exact rule map risk→routing принадлежит separately approved compatible Risk + Qualification rule, не Risk Policy; Risk alone никогда не создаёт `QUALIFIED_HYPOTHESIS`/`REJECTED_BY_MATCHING`, а exact trigger/mapping остаются `OPEN` под `XFR-D-M2`/`XFR-D-055`.

#### `MRP-C-008` — reviewer применяет approved policy, не меняет threshold ad hoc
**Given:** `XFR-D-053 v1.0` и применимый legally significant Risk-review case. **When:** `HUMAN_REVIEW_REQUIRED` направляет case на review и reviewer принимает решение. **Then:** request не становится confirmation/queue assignment/Decision Record/legal или access outcome; reviewer применяет approved version/hash policy в пределах действующего письменного назначения/scope и least-privilege RBAC, не меняет threshold/criticality/mapping/evidence sufficiency/routing ad hoc, проходит conflict check до открытия case/любого evidence access и повторно непосредственно перед решением. Legal/Decision Service остаётся sole outcome writer, Matching calculation immutable, queue/SLA non-authoritative; exact appointment/RBAC/queue/outcome/schema/appeal/carrier остаются `OPEN`. Payer-specific second-level confirmation и separately applicable four-eyes reviewer≠approver — разные controls, не universal quorum; §21.7 не импортирует dispute schema, `XFR-D-041` distinct.

#### `MRP-C-009` — абсолютный запрет protected/proxy; правильная роль LEGAL review
**Given:** risk category потенциально использует признак, который может быть protected attribute или hidden proxy. **When:** признак рассматривается для использования в Risk Score. **Then:** `XFR-D-054 v1.0` разрешает только `PARTIALLY_RESOLVED_BOUNDARY`: protected/proxy classification выполняется первой; только positively confirmed non-protected/non-proxy candidate переходит к отдельному `LEGAL` lawful-source/lawful-basis determination для exact feature/source/use/purpose/version. Confirmed protected/proxy исключается безусловно; lawful basis не отменяет ban §17. `LEGAL + PRODUCT` governance owner is candidate-derived, while `LEGAL` separately remains source-normative lawful authority. Missing/unclassified material blocks affected use without adverse inference; AI/heuristics/diagnostics/`ACTIVE` create no legal determination. No actual taxonomy/classification/lawful determination or fairness standard is approved.

#### `MRP-C-010` — geography — candidate, метод/threshold re-identification не изобретён
**Given:** risk-категория «повторная идентификация» рассматривает возможный input. **When:** формируется diagnostic. **Then:** coarse geography — один из возможных input candidates (Feature Schema §7.4), не обязательный механизм; любая detection technique — `DECISION_CANDIDATE_FOR_REVIEW`/`OPEN`, не established mechanism; численный threshold не изобретается; protected-contour запрет сохраняется без ослабления.

#### `MRP-C-011` — self-report не становится ground truth
**Given:** единственный источник — self-report стороны. **When:** оценивается достаточность evidence. **Then:** `XFR-D-049 v1.0` сохраняет contextual rule: одного self-report недостаточно только там, где independently applicable approved rule требует verification/human decision (§27.2); self-report не объявляется universally insufficient, не становится ground truth и не получает hidden sufficiency level. Exact mapping/level/criticality remains `OPEN`.

#### `MRP-C-012` — outcome correlation ≠ causation; Paused и corrected/superseded outcomes не создают leakage
**Given:** Risk calibration ссылается на outcome history через `MATCHING_EVALUATION_PLAN`. **When:** формируется calibration evidence. **Then:** корреляция не представляется как причинность; `Paused` не используется как terminal outcome (`CO-C-004`); superseded/corrected записи обрабатываются по правилам `CAMPAIGN_OUTCOMES.md`/Evaluation Plan (`CO-C-016/019/026`, Evaluation Plan §5.3–5.4), не переопределяются заново этим Risk Policy.

#### `MRP-C-013` — synthetic-to-production правило честно классифицировано как candidate
**Given:** Risk calibration построена на synthetic dataset (Evaluation Plan §3, категории 1–4). **When:** формулируется заключение о production readiness. **Then:** `XFR-D-050 v1.0` human-approves only the qualitative boundary that synthetic-only evidence creates no production calibration/readiness claim. This does not approve any dataset, manifest, run, result, metric, threshold, policy, production use or gate; exact contents remain `OPEN`.

#### `MRP-C-014` — нет invented Risk weights/thresholds/TTL
**Given:** любой раздел документа. **When:** выполняется поиск конкретного числа. **Then:** ни один invented численный weight/threshold/TTL для Risk calibration/aggregation не найден вне явно помеченного `OPEN_BLOCKED_PENDING_DECISION`; уже существующие source-normative числа из других разделов Architecture (например, §34.1 0%-инварианты про unknown-as-negative) допустимо цитировать только как внешние поведенческие инварианты, не как Risk calibration target.

#### `MRP-C-015` — per-category visibility и non-compensation qualitatively resolved by `XFR-D-048`; audit bundle остаётся отдельным SOURCE_NORMATIVE baseline
**Given:** утверждённый qualitative aggregation boundary `XFR-D-048 v1.0` (multi-component representation + conditional non-compensation invariant для отдельно классифицированных critical категорий). **When:** формируется итоговый Risk output. **Then:** общий audit-bundle §33 (`SOURCE_NORMATIVE`) уже требует сохранения активных критериев/причин исключения/конфликтов для любого расчёта Matching Engine — не специфично для Risk; per-category Risk detail остаётся non-compensating и видимым по `XFR-D-048`. `XFR-D-047` разрешает только representation discipline and scalar-supplement-only semantics; `XFR-D-049` — contextual sufficiency discipline без critical classification; `XFR-D-050` — calibration evidence prerequisites без dataset/result approval; `XFR-D-051` — distinct source-behavior preservation без exact operational mapping; `XFR-D-052` — internal opaque reason-reference governance; `XFR-D-054` — protected/proxy→lawful two-stage governance без actual determination; `XFR-D-055` — read-only hand-off/interface semantics. Все имеют только `PARTIALLY_RESOLVED_BOUNDARY`. Для duplication `XFR-D-056 v1.0` не классифицирует possible или confirmed duplicate как critical: `XFR-D-048` non-compensation применяется только после independently approved critical classification. Численная формула, runtime representation, critical-category mapping, exact reason namespace/values/mappings, exact Risk→Qualification trigger/mapping/carrier and all exact data/statistical/legal contents remain `OPEN` under applicable records and `XFR-D-M2`. Authoritative dependency split зафиксирован в accepted records и Inventory §5.6–§5.6.5.

#### `MRP-C-016` — non-deterministic external component не проходит gate самостоятельно
**Given:** внешний вероятностный компонент участвует в Risk. **When:** оценивается допуск к Matching Qualification Gate. **Then:** merged `XFR-D-M4 v1.0` сохраняет компонент только как advisory signal: он не проходит gate самостоятельно; recorded и bounded probabilistic replay distinct, bounded mode требует separately approved closed immutable version/hash-bound candidate and frozen/preregistered evidence, а every exact tolerance/invariant/data/statistical/policy/production/carrier/runtime content remains `OPEN`.

#### `MRP-C-017` — evaluation не обновляет runtime policy автоматически
**Given:** Risk Policy candidate threshold найден через `MATCHING_EVALUATION_PLAN` или завершён replay. **When:** evaluation/replay завершён. **Then:** runtime rules не меняются автоматически (§34.4: «Автоматическое изменение продуктивных правил по результатам обучения — 0 случаев»; согласовано с Evaluation Plan `MEP-C-014/018`); `XFR-D-050 v1.0` сохраняет baseline-first, tuning/final isolation, compatible comparison, uncertainty/separate-segment/fairness reporting and no-automatic-action boundary without approving data/results; `XFR-D-M4 v1.0` сохраняет historical Match Result immutable и также не создаёт automatic policy/model/runtime/gate action.

#### `MRP-C-018` — Safe Presentation использует Risk-related classification как input, не владеет Risk; SLO/Cost вне scope
**Given:** presentation-правило или operational metric рассматривается в контексте Risk. **When:** формируется scope документа. **Then:** `SAFE_PRESENTATION_POLICY` не подменяется Risk Policy: может использовать Risk-related classification как input для собственного presentation-решения, но не вычисляет и не владеет Risk calculation; SLO/Cost (§51/§54) остаются `OUT_OF_SCOPE`.

---

## 15. DoD и последствия

Настоящий документ:

- пригоден только для cross-functional review (AI + LEGAL + PRODUCT + DEVELOPMENT, по применимости каждого раздела);
- не закрывает Architecture §37 вопрос №8 полностью: только qualitative governance/evidence boundary имеет `PARTIALLY_RESOLVED_BOUNDARY`, а exact trigger/threshold/mapping/route contents остаются `OPEN`;
- синхронизирует merged `MRP-04 + MQP-09 → XFR-D-M2` only as `PARTIALLY_RESOLVED_BOUNDARY` for exact roles, semantic separation, two permitted candidate directions without selection, immutable candidate/evidence discipline, affected-progression fail-closed, non-compensation/precedence/multi-cause/history and no-automatic-action safeguards; every exact trigger/threshold/comparator/mapping/route/data/statistical/policy/production/carrier/runtime/implementation content remains `OPEN`;
- сохраняет 14-row register и merged `EP-10 + MRP-11 + MQP-15 + MSP-14 → XFR-D-M4` с четырьмя `PRIMARY_MERGED_MEMBER`; синхронизирует row №11 только как `PARTIALLY_RESOLVED_BOUNDARY`, при этом exact tolerance/invariant/data/statistical/policy/production/carrier/runtime/implementation contents remain `OPEN`;
- синхронизирует G3 standalone `MRP-01 → XFR-D-047`, `MRP-03 → XFR-D-049`, `MRP-05 → XFR-D-050`, `MRP-06 → XFR-D-051`, `MRP-09 → XFR-D-054` only as `PARTIALLY_RESOLVED_BOUNDARY` for their approved representation/evidence/calibration/source-state/protected-lawful qualitative boundaries and exact role splits; Risk Policy artifact owner `Chief AI Architect + LEGAL`, separate source-normative `LEGAL` lawful authority, all canonical identities and the 14-row register remain unchanged; every exact identifier/enum/mapping/criticality/number/dataset/statistical/result/lawful determination/reviewer/RBAC/schema/carrier/policy/production/runtime/implementation content remains `OPEN`;
- не переводит `IMPLEMENTATION_READINESS_GATE`, `SYNTHETIC_ACCEPTANCE_GATE` или `PRODUCTION_LAUNCH_GATE` в иной статус — все три `BLOCKED`;
- не синхронизирует Controlled Artifact Manifest — запись `MATCHING_RISK_POLICY` не добавляется до реального утверждения;
- не содержит ни одного численного Risk weight/threshold/TTL/calibration target, ни одного выбранного numeric aggregation/duplication algorithm, ни одного public/runtime enum или reason-code каталога; qualitative aggregation/non-compensation boundary утверждена `XFR-D-048 v1.0`, Risk reason-reference governance boundary частично разрешена `XFR-D-052 v1.0`, reviewer-authority/queue-non-authority/Decision-linkage boundary — `XFR-D-053 v1.0`, qualitative Risk→Qualification governance/interface boundary — `XFR-D-055 v1.0`, а duplication-detection governance/authority boundary — `XFR-D-056 v1.0`; exact namespace/codes/values/order/severity/mappings/compatibility/fallback/TTL/hash fields/trigger, entity taxonomy/keys/algorithm/features/threshold/confidence/evidence/reviewer appointments/RBAC/queue/SLA/outcomes/Decision schema/appeal/merge-split, schema/carrier/data/policies/manifest/runtime/implementation остаются `OPEN`, и сам Proposal остаётся `Proposal`, не `APPROVED`;
- не ослабляет protected/proxy prohibition Architecture §17 ни в каком виде;
- не разрешает и не инициирует implementation, model release, реальные данные или production launch;
- не изменяет ни один существующий файл, включая Architecture, Feature Schema, Evaluation Plan, Data Contracts, controlled-set artifacts, любой PR.
